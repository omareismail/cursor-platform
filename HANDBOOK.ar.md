# دليل منصة cursor-platform — الشرح الكامل بالعربية

> النسخة الإنجليزية: [HANDBOOK.md](HANDBOOK.md) · هذا الملف مكافئ لها ويغطي
> طبقة دورة الحياة الجديدة أيضاً.
> آخر تحديث: 6 سبتمبر 2026.

---

## 1. ما هي هذه المنصة؟

`cursor-platform` ليست مشروعاً برمجياً — هي **طبقة تشغيل للوكيل الذكي** توضع
فوق مشاريعك الحقيقية. مهمتها أن تجعل الوكيل (Cursor أو Claude Code) يعمل على
كود **.NET + React** في سياق التقنية المالية الخليجية بانضباط: يكتشف بنية
المستودع الحقيقية، يقلّد الأنماط الموجودة فيه بدل اختراع أنماط جديدة، ويحترم
قواعد المعمارية والأمان ومسار التدقيق وتخطيط RTL/العربية.

الفكرة المركزية في جملة واحدة:

> **القاعدة المكتوبة نصاً نصيحة؛ القاعدة المنفَّذة بـ hook أمر.**
> كل ما استطاعت المنصة تحويله من نصيحة إلى إنفاذ ميكانيكي، حوّلته.

### الأرقام

| العنصر | العدد | أين |
|---|---|---|
| مهارات (skills) | 97 | `.cursor/skills/` |
| قواعد حراسة (rules) | 12 | `.cursor/rules/` |
| وكلاء فرعيون (subagents) | 14 | `.claude/agents/` |
| خطّافات إنفاذ (hooks) | 7 | `.claude/hooks/` |
| أدوات تحقّق (tools) | 18 | `.cursor/tools/` |
| ملفات الذاكرة (memory-bank) | 24 | `memory-bank/` |
| مراحل دورة الحياة | 6 | `.cursor/lifecycle/gates/` |

### لمن هذا الدليل؟

- من يفتح المستودع لأول مرة ويريد فهم كل ملف فيه.
- من يريد تركيب المنصة على مشروع قائم أو جديد.
- من يريد إضافة مهارة أو قاعدة دون كسر التوازن الموجود.

---

## 2. البدء السريع

### على المنصة نفسها (هذا المستودع)

```bash
# 1. افتح المجلد في Cursor أو Claude Code
# 2. اقرأ العقد الخاص بوكيلك:
#    Cursor       -> AGENTS.md
#    Claude Code  -> CLAUDE.md   (يستورد AGENTS.md ويضيف الوكلاء والخطّافات)
# 3. تحقّق أن كل شيء متسق
node .cursor/tools/docs-lint.mjs check
node .cursor/tools/build-plugin.mjs check
```

### على مستودع تطبيق حقيقي

```bash
# مشروع جديد من فكرة
node .cursor/tools/lifecycle.mjs init --name "اسم المنتج"
/product-brief   وصف الفكرة في جملة

# مستودع قائم فيه كود بالفعل
node .cursor/tools/lifecycle.mjs init --name "اسم المنتج" --existing
/repo-discovery full
/context-sync
/feature-inventory full
```

الدليل التشغيلي الكامل خطوة بخطوة موجود في
[`.cursor/docs/IDEA-TO-PRODUCTION.md`](.cursor/docs/IDEA-TO-PRODUCTION.md).

---

## 3. خريطة المجلد

```
cursor/
│
├── AGENTS.md              عقد الوكيل — Cursor يقرأ هذا
├── CLAUDE.md              نقطة دخول Claude Code — يستورد AGENTS.md ويضيف
│                          الوكلاء والخطّافات والقواعد دائمة التفعيل
├── HANDBOOK.md            الدليل الكامل (إنجليزي)
├── HANDBOOK.ar.md         هذا الملف
├── README.md              نظرة عامة + التركيب
├── .mcp.json              خوادم MCP للمشروع (مُلتزَم به؛ متغيرات بيئة فقط، لا أسرار)
├── .gitignore
│
├── .cursor/               ← المصدر الوحيد للحقيقة
│   ├── skills/            97 مهارة — كل واحدة مجلد فيه skill.md
│   ├── rules/             12 قاعدة .mdc (5 عامة + 7 مرتبطة بأنماط ملفات)
│   ├── lifecycle/gates/   6 تعريفات بوابات — معايير كل مرحلة
│   ├── docs/              التوثيق: الخرائط، الفهارس، الحوكمة
│   ├── tools/             18 أداة .mjs للتحقّق والقياس
│   ├── cache/             repo-map.json + feature-map.json (مولّدة، مستثناة من git)
│   └── settings.local.json  إعدادات Cursor المحلية (غير مُلتزَم بها)
│
├── .claude/               ← طبقة Claude Code
│   ├── skills/            96 shim مولّد — يشير لـ .cursor/skills/ (لا تعدّله يدوياً)
│   ├── skills/_descriptions.json   أوصاف مكتوبة يدوياً تتفوّق على الاستخراج التلقائي
│   ├── agents/            14 وكيلاً فرعياً للقراءة فقط (عزل السياق)
│   ├── hooks/             7 خطّافات إنفاذ + sync-skills.mjs + _lib.mjs
│   └── settings.json      ربط الخطّافات + قوائم السماح/المنع
│
├── .claude-plugin/        بيان الـ marketplace والبلجن
├── plugin/                شجرة البلجن القابلة للتوزيع (مولّدة — لا تعدّلها يدوياً)
│
├── memory-bank/           24 ملفاً — ذاكرة المشروع على طبقتين
├── lifecycle/             حالة دورة حياة المنتج (state.json يُنشأ في مستودع التطبيق)
├── templates/             بوابات البناء لمستودع التطبيق الهدف
└── flutter-platform/      منصة موازية لـ Flutter (مستقلة، خارج نطاق هذا الدليل)
```

### قاعدة التحرير الذهبية

> المهارات تعيش في **`.cursor/skills/`** فقط.
> بعد إضافة أو إعادة تسمية أو حذف مهارة:
> ```bash
> node .claude/hooks/sync-skills.mjs
> ```
> الـ CI يُفشل الـ PR إذا انحرفت الـ shims.

---

## 4. النموذج الذهني — أربع طبقات

فهم المنصة يصير سهلاً إذا نظرت إليها كأربع طبقات، كل واحدة أقوى من التي فوقها:

```
  ١  العقد            AGENTS.md / CLAUDE.md
     "هكذا تعمل"       ← الوكيل يقرأها في بداية كل جلسة
          │
          ▼
  ٢  المهارات          .cursor/skills/*/skill.md
     "كيف تنفّذ X"      ← إجراءات مكتوبة، يستدعيها المستخدم بـ /اسم-المهارة
          │
          ▼
  ٣  القواعد           .cursor/rules/*.mdc
     "ما لا يجوز"       ← نص مُلزِم، يقرؤه النموذج ويلتزم به
          │
          ▼
  ٤  الإنفاذ           .claude/hooks/*.mjs + templates/ + CI
     "ما لا يمكن"       ← كود يعيد exit code 2 ويوقف العملية
```

الفرق بين الطبقتين ٣ و٤ هو جوهر المنصة. الطبقة الثالثة تحميك من نموذج
يتصرّف بحسن نية لكنه نسي؛ الطبقة الرابعة تحميك من الجميع — بما فيهم أنت في
يوم متعب، والنموذج في يوم سيئ.

### مثال عملي على الفرق

| الادّعاء | نصيحة (قاعدة) | إنفاذ (خطّاف/CI) |
|---|---|---|
| لا تكتب سراً في الكود | `04-security-guard.mdc` | `guard-write.mjs` يمنع الكتابة |
| لا تثبّت حزمة جديدة | `10-evidence-and-dependency-guard.mdc` | `guard-bash.mjs` يمنع الأمر |
| لا تكتب كوداً قبل الموافقة على التصميم | `11-lifecycle-gate.mdc` | `guard-phase.mjs` يمنع الكتابة |
| احترم حدود الطبقات | `02-dotnet-architecture-guard.mdc` | `templates/dotnet/ArchitectureTests/` يُفشل البناء |

---

## 5. دورة حياة المنتج — المراحل الست

هذه هي الطبقة التي تحوّل المنصة من "مجموعة مهارات" إلى **مصنع برمجيات**:
فكرة تدخل، ومنتج في الإنتاج يخرج، ولا يمكن تخطّي مرحلة.

### حلقتان متداخلتان، لا ستّ خطوات

```
دورة حياة المنتج (11-lifecycle-gate.mdc)              مرّة واحدة لكل منتج
  ١ المتطلبات ← ٢ التحليل ← ٣ التصميم ← ٤ التطوير ⇄ ٥ الاختبار ← ٦ الإنتاج
      بوابة       بوابة       بوابة        بوابة      بوابة       بوابة
                                ▲                │
                    البوابة الوحيدة              │
                    التي خلفها hook              ▼
                                    مسار الميزة (01-specify-rules.mdc)
                                        مرّة لكل ميزة
                  analyze ← clarify ← constitution ← plan ← specify
                        ← implement ← checklist ← commit
```

**نقطة مهمة:** المرحلتان ٤ و٥ **حلقة** لا خطوتان. فشل اختبار يعيد العمل إلى
التطوير، لكن دورة الحياة تبقى في `TESTING` ولا ترجع للخلف.

### ملف الحالة

`lifecycle/state.json` هو الإجابة الوحيدة على سؤال "في أي مرحلة نحن؟"
مملوك للآلة — `guard-write.mjs` يمنع تعديله يدوياً، لأن تعديله باليد يسمح
باعتماد مرحلة دون وجود مُخرَجاتها، وهذا بالضبط الفشل الذي بُنيت البوابات لمنعه.

```bash
node .cursor/tools/lifecycle.mjs product                    # ابدأ هنا: إحاطة بشاشة واحدة
node .cursor/tools/lifecycle.mjs status                     # أين نحن، وهل ما زال صحيحاً
node .cursor/tools/lifecycle.mjs check [PHASE]              # شهادة ١: المُخرَجات موجودة
node .cursor/tools/lifecycle.mjs gate PHASE                 # من يحقّ له الحكم، ولماذا هو
node .cursor/tools/lifecycle.mjs record-gate PHASE --verdict GO --by "<المُراجِع>"
node .cursor/tools/lifecycle.mjs approve PHASE --by "اسمك"  # شهادة ٣: الإنسان
node .cursor/tools/lifecycle.mjs advance
node .cursor/tools/lifecycle.mjs rollback PHASE --reason "السبب"
node .cursor/tools/lifecycle.mjs override PHASE --reason "..." --risk HIGH --by "اسمك" --expires 7
node .cursor/tools/lifecycle.mjs init --name "المنتج" [--existing]
```

خطّاف `SessionStart` يحقن المرحلة الحالية تلقائياً في بداية كل جلسة.
مستودع بلا `lifecycle/state.json` ببساطة لم يتبنَّ دورة الحياة — وهذا وضع
صحيح، وكل هذه الطبقة تصبح خاملة فيه.

### ثلاث موافقات لكل بوابة

البوابة تُفتح فقط عندما تتفق الثلاثة. ولا واحدة منها تكفي وحدها:

| الموافقة | تُسجَّل بـ | ماذا تُثبت |
|---|---|---|
| ميكانيكية | `approve`، يُعاد حسابها لحظتها | المُخرَجات موجودة وليست قوالب فارغة. أجزاء من الثانية. لا تُجادَل. |
| حُكميّة | `/lifecycle-gate` ← `record-gate` | المُخرَجات تستحق البناء عليها، وفق معايير `.cursor/lifecycle/gates/`. |
| بشرية | `approve --by "اسم"` | يوجد شخص مسؤول عن القرار. |

`approve` يرفض ما لم تكن الثلاث موجودة، **وما لم تكن المرحلة هي الحالية،
وما لم تُعتمَد سابقتها**. **بوابة تستطيع الآلة فتحها وحدها ليست بوابة.**

الموافقة الميكانيكية **تُعاد حسابها عند الاعتماد**، ولا تُؤخذ من فحص سابق —
فحصٌ نجح قبل ساعة لا يقول شيئاً عن الآن.

### ثلاث موافقات تعني ثلاثة **أطراف** — لا طرفاً واحداً ثلاث مرّات

كان في النسخة الأولى عيبٌ مكتوب بخطّ اليد في وصف كل وكيل: `product-manager`
يكتب المتطلبات **ويحكم** على بوابة ١، و`solution-architect` يختار المعمارية
**ويحكم** على بوابة ٣. هذه ليست مراجعة — هذه الشهادة الأولى موقّعة مرّتين.

والسبب ليس سوء نيّة ولا تهاوناً. **الكاتب حين يعيد قراءة عمله تكون كل أسبابه
ما تزال حاضرة في سياقه**، فيقرأ ما قصده لا ما كتبه، ولا يعثر أبداً على الشيء
الذي لم يخطر له في المرّة الأولى. لا يُصلح هذا مزيدٌ من الحرص.

فصار لكل بوابة **مُراجِع مُعلَن داخل ملف البوابة نفسه**، ولا يكون أبداً أحد
كُتّاب تلك المرحلة:

| البوابة | من كتبها | من يحكم عليها | لماذا هو تحديداً |
|---|---|---|---|
| ١ المتطلبات | `product-manager` · `ux-bridge` | `business-analyst` | المرحلة ٢ ترث كل غموض يبقى فيها، فليجده الآن وهو يكلّف جملة |
| ٢ التحليل | `business-analyst` | `solution-architect` | يرث النموذج كاملاً: ثابتٌ خاطئ هنا يصير قيداً خاطئاً في المخطّط |
| ٣ التصميم | `solution-architect` · `ux-bridge` | `security-auditor` | آخر نقطة يكلّف فيها التهديد فقرةً بدل حادثة — ومعماريٌّ يدافع عن اختياره لا يراجعه |
| ٤ التطوير | خطّ speckit | `test-engineer` | من سيكتب الاختبار هو من يقرّر أن التطوير انتهى |
| ٥ الاختبار | `test-engineer` | `product-manager` | كاتب معايير القبول وحده يعرف ماذا كان يقصد بها، ولم يكتب اختباراً واحداً فهو حرّ في أن يقول "لا" |
| ٦ الإنتاج | `ops-reviewer` | `security-auditor` | كاتب الـ runbook أسوأ من يحكم هل يستطيع غريبٌ اتّباعه في الثالثة فجراً |

وثلاثة أقفال تجعل هذا واقعاً لا نصيحة:

1. **`record-gate` يرفض حكماً باسم غير المُراجِع المُعلَن**، وإن كان الاسم أحد
   كُتّاب المرحلة قال له الرفض ذلك صراحةً.
2. **`approve` يرفض توقيعاً بنفس اسم من سجّل الحكم.** شهادتان بتوقيع واحد
   شهادةٌ واحدة.
3. **الحكم مربوط ببصمات المستندات التي قرأها المُراجِع فعلاً.** عدِّل مستنداً
   بين المراجعة والتوقيع، ويُرفض التوقيع: الـ GO كان على مسوّدة أخرى. هذه كانت
   الثغرة الوحيدة الباقية — الـ GO كان ينتقل بصمت إلى نصٍّ لم يقرأه أحد.

وعند التوقيع يطبع `approve` قبل أي شيء **كم مرّة رُفضت هذه المرحلة بـ NO-GO
قبل هذا الـ GO**. ليس رفضاً — مرحلةٌ تُصلَح بعد رفضين أمرٌ سليم — لكن "نجحت
من المحاولة الثالثة" حقيقة مختلفة عن "نجحت"، واسمك يوقّع على إحداهما.

> **ما لا يثبته هذا:** لا شيء في مستودع يستطيع أن يثبت أن المُراجِع قرأ فعلاً.
> ما يفعله هذا الفصل أنه يجعل حكم الكاتب على نفسه **شيئاً تفعله عن قصد**، بعد
> أن كان هو السلوك الافتراضي. والاستقلال الحقيقي يأتي من تشغيل المُراجِع
> **كوكيل فرعي جديد** لا يحمل في سياقه محادثة الكتابة.

### الإحاطة — `lifecycle.mjs product`

شاشة واحدة: المرحلة وسُلّم البوابات، **ملف الحوكمة**، حزمة التقنيات، التكاملات،
مُلّاك المراحل، وأي طلب تغيير مفتوح أو تجاوز فعّال.

**ولا سطر فيها مُعلَن يدوياً.** المرحلة من `state.json`، والحزمة من
`memory-bank/technologyStack.md`، والتكاملات من `.mcp.json`، وأعلام الحوكمة
مشتقّة من مستندات المرحلة الأولى. اقتراحك الأصلي كان ملفاً يُكتب باليد يجمع
الاسم والحزمة والمرحلة — وتلك **نسخة رابعة** من حقائق لها مالك بالفعل، والنسخة
شيء يخطئ بصمت.

الجزء الذي لم يكن له بيت فعلاً هو **ملف الحوكمة**، وهو الذي يغيّر ما يُفرَض:

| العَلَم | يُفعّل |
|---|---|
| `money` | القاعدة 07 صارمة، `decimal` إلزامي، مسار تدقيق مطلوب |
| `PII` | `/threat-model` إلزامي في البوابة ٣، تصنيف البيانات مطلوب |
| `auth` | المصادقة والتخويل يُصمَّمان لا يُسمَّيان |
| `regulated` | `/compliance-audit` ينطبق، مع تسمية الأنظمة |

وإن قالت الأداة إن الأعلام لا يمكن اشتقاقها، فالمرحلة الأولى لم تنتج مستنداً
مقروءاً بعد — قُل ذلك، لا تفترض أن الجواب «لا».

### الحالة مشتقّة لا مخزّنة

الملف يحفظ الشهادات؛ والحالة تُحسب منها عند كل قراءة. لو خُزّنت `STALE`
لاحتجنا رايةً ثانية تقول هل ما زالت `STALE` صحيحة.

| الحالة | المعنى |
|---|---|
| `NOT_STARTED` / `IN_PROGRESS` | لا موافقة بعد |
| `APPROVED` | الشهادات الثلاث موجودة وما زالت صالحة |
| `INHERITED` | مستودع قائم؛ نُفّذت المرحلة بشكل غير رسمي قبل تبنّي دورة الحياة |
| `STALE` | **كانت** معتمَدة، ثم تغيّر شيء اعتُمدت عليه |
| `BLOCKED` | مرحلة سابقة غير مفتوحة |

الاعتماد يبصم كل مُخرَج مطلوب ويختم إصدار ملف البوابة نفسه. فالنتيجة:

```
اعتُمد التصميم  ←  أحدهم عدّل docs/design/api-design.md
                ←  التصميم يصير STALE في أول أمر تالٍ
                ←  guard-phase.mjs يعود لحظر الكتابة في src/
                ←  المراحل اللاحقة تصير BLOCKED
```

بلا كانس ولا ذاكرة مؤقتة ولا شيء يحتاج إبطالاً. أعِد التعديل تتطابق البصمة
وتعود `APPROVED` — الشهادات لم تُرمَ أصلاً.

### تجاوزات مسجَّلة، لا `--force`

لا توجد راية إجبار. التجاوز **سجلّ**:

```bash
node .cursor/tools/lifecycle.mjs override DESIGN \
  --reason "عرض للعميل يوم الأحد" --risk HIGH --by "Ezzdeen" --expires 3
```

له معرّف ومالك ودرجة خطر وقائمة بما تجاوزه بالضبط ومدّة أقصاها 90 يوماً.
وعند انتهائها تصير المرحلة `STALE` وحدها، وCI يفشل على تجاوز منتهٍ.
التجاوز الدائم بوابة محذوفة.

### المراحل الست بالتفصيل

| # | المرحلة | المالك (وكيل) | تُنتج | البوابة |
|---|---|---|---|---|
| ١ | المتطلبات | `product-manager` | `docs/product/` — brief, personas, prd, nfr, scope, story-map | [01](.cursor/lifecycle/gates/01-requirements.gate.md) |
| ٢ | التحليل | `business-analyst` | `docs/analysis/` — domain-model, use-cases, workflows, business-rules, risks | [02](.cursor/lifecycle/gates/02-analysis.gate.md) |
| ٣ | التصميم | `solution-architect` + `ux-bridge` | `docs/design/` — architecture, api-design, database-design, security-design, adr, ux | [03](.cursor/lifecycle/gates/03-design.gate.md) |
| ٤ | التطوير | الخيط الرئيسي | `src/`, `frontend/`, `specs/features/`, الترحيلات | [04](.cursor/lifecycle/gates/04-development.gate.md) |
| ٥ | الاختبار | `test-engineer` | `docs/testing/strategy.md`, `tests/` | [05](.cursor/lifecycle/gates/05-testing.gate.md) |
| ٦ | الإنتاج | `ops-reviewer` | `.github/workflows/`, أدلة التشغيل، SLOs، خطة التحويل | [06](.cursor/lifecycle/gates/06-production.gate.md) |

مُلّاك المراحل **وكلاء للقراءة فقط**، مثل بقية الوكلاء في `.claude/agents/`.
يقرؤون كثيراً ويعيدون خلاصة؛ الكتابة تحدث في الخيط الرئيسي حيث تُطبَّق
القواعد والخطّافات.

### بوابة التصميم — الوحيدة المنفَّذة بكود

ما دامت مرحلة التصميم تُشتقّ إلى غير `APPROVED` أو `INHERITED` — و`STALE`
منها — فإن `guard-phase.mjs` يعيد `exit 2` على أي كتابة تحت `src/` أو
`backend/` أو `frontend/`.

**غير ممنوع إطلاقاً:** `tests/`, `specs/`, `docs/`, `memory-bank/`,
`.cursor/`, `.claude/`, `.github/` — كتابة اختبار أو مواصفة قبل بوابة التصميم
ممارسة جيدة لا مخالفة.

**مخرج الطوارئ:** `LIFECYCLE_OVERRIDE=1` — نفس نمط `CLAUDE_ALLOW_TIER2_EDIT`.
يضبطه إنسان عن قصد. اقتراح الوكيل لاستخدامه هو بالضبط الفشل الذي وُجدت
البوابة لمنعه.

### الترقية (Promotion) — لا ازدواجية

المراحل ١–٣ يجب ألا تصنع بيتاً ثانياً للحقيقة التي يحفظها `memory-bank/`
أصلاً. كل بوابة **تُرقّي** قراراتها إلى ملفات الطبقة الثانية التي تقرؤها
القواعد:

| المرحلة | تُرقّي إلى |
|---|---|
| ١ | `projectbrief.md`, `productContext.md`, `businessRules.md` |
| ٢ | `businessRules.md`, `glossary.md` (مصطلحات عربي/إنجليزي) |
| ٣ | `architecture.md`, `technologyStack.md`, `databaseConventions.md`, `apiConventions.md`, `securityStandards.md` |

**هذه هي النقطة كلها.** قرار في `docs/design/architecture.md` مجرّد وثيقة؛
نفس القرار في `memory-bank/architecture.md` تفرضه القاعدة `02` على كل ملف
`.cs` لبقية عمر المشروع. تخطَّ الترقية ويبقى التصميم ورقة؛ نفّذها ويصبح
التصميم خطأ ترجمة.

ملفات الطبقة الثانية يكتبها البشر، و`guard-write.mjs` يمنع الوكيل من الكتابة
فيها. لذلك الترقية خطوة صريحة: اعرض الفرق (diff)، خذ موافقة المستخدم، ثم
طبّق بـ `CLAUDE_ALLOW_TIER2_EDIT=1`. **أبداً بصمت.**

### المستودعات القائمة (Brownfield)

مستودع فيه كود بالفعل لم يتخطَّ المراحل ١–٣ — نفّذها بشكل غير رسمي، قبل
سنوات، في رؤوس الناس.

```bash
node .cursor/tools/lifecycle.mjs init --name "المنتج" --existing
```

يُعلّم المراحل ١–٣ بـ `INHERITED`، وهذا **يفتح بوابة التصميم** ويبدأ المنتج
من `DEVELOPMENT` — لأن منع كل عمل على نظام حيّ سيكون عبثاً. لترقية مرحلة من
`INHERITED` إلى `APPROVED`، أنتج مُخرَجاتها (`/feature-inventory` و
`/context-sync` يعيدان بناء معظم المرحلتين ١–٢ من الكود) ثم `check` ثم
`approve`.

---

## 6. المهارات الـ97

### كيف تُستدعى المهارة

المهارة إجراء مكتوب في `.cursor/skills/<الاسم>/skill.md`، يُستدعى بـ
`/<الاسم>`. الوكيل **يعلن** المهارة قبل تشغيلها بهذه الصيغة:

> **Matched skill:** `اسم-المهارة` — وصف في سطر واحد.

### الفئات الخمس — تحدّد هل ينتظر الوكيل إذنك

| الفئة | المعنى | السلوك |
|---|---|---|
| **A** | تُنشئ أو تعدّل ملفات | تُعلن ثم **تنتظر** موافقتك |
| **B** | تحليل وتدقيق للقراءة فقط | تُعلن ثم تتابع تلقائياً |
| **C** | مخططات ووثائق | تُعلن ثم تتابع تلقائياً |
| **D** | صيانة مساحة العمل | تعمل بصمت، بلا إعلان |
| **E** | مسار المواصفات (speckit) | تُعلن المرحلة ثم **تنتظر** |

الفهرس الكامل: [`.cursor/docs/skill-catalog.md`](.cursor/docs/skill-catalog.md) ·
خريطة "أريد أن أفعل X": [`.cursor/docs/START-HERE.md`](.cursor/docs/START-HERE.md)

---

### ٦.١ التحكّم في دورة الحياة (2)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `lifecycle` | A | نقطة الدخول: أين نحن، ابدأ، تقدّم، تراجع |
| `lifecycle-gate` | B | تحكم على مُخرَجات المرحلة وتعيد GO / NO-GO مانعاً |

### ٦.٢ المرحلة ١ — المتطلبات (4)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `product-brief` | A | تحوّل جملة نيّة واحدة إلى بيان مشكلة محدود، مع نطاق مضاد صريح |
| `persona-gen` | A | شخصيات المستخدمين: المهمة المطلوبة، الجهاز، اللغة، وما الذي يجعلهم ينسحبون |
| `product-requirements` | A | وثيقة PRD: متطلبات وظيفية، ومتطلبات غير وظيفية **مُكمَّمة برقم**، وحدود MVP |
| `user-story-map` | A | رحلات وقصص بمعايير قبول Given/When/Then يستطيع الاختبار أن يؤكّدها |

### ٦.٣ المرحلة ٢ — التحليل (4)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `domain-model-gen` | A | الكيانات، التجمّعات (aggregates)، الثوابت (invariants)، ومخطط ERD بـ Mermaid |
| `use-case-gen` | A | حالات الاستخدام بمساراتها البديلة والاستثنائية + التتبّع للقصص في الاتجاهين |
| `business-rules-gen` | A | فهرس مرقّم واحد لكل قواعد العمل، جاهز للترقية إلى `businessRules.md` |
| `risk-register` | A | سجلّ المخاطر: لكل خطر مالك ومرحلة تُنهيه — أوسع من `/threat-model` |

### ٦.٤ المرحلة ٣ — التصميم (4 جديدة + الموجود)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `solution-architecture` | A | تختار المعمارية من بدائل مُكلَّفة، وتتبع كل NFR إلى الآلية التي تحقّقه |
| `api-contract-design` | A | فهرس نقاط النهاية + الأعراف المقرّرة مرّة واحدة (ترقيم الصفحات، الأخطاء، الإصدارات) |
| `data-model-design` | A | المخطط الفيزيائي، الفهارس، أدوار مزوّدي قواعد البيانات، مسار الترحيل الحيّ |
| `ux-design-bridge` | A | جرد الشاشات، كل حالاتها، عقد RTL/التعريب، رموز التصميم؛ يقرأ خادم Figma MCP |
| `threat-model` | B | نمذجة تهديدات STRIDE قبل وجود الكود وبينما الإصلاح ما زال قراراً تصميمياً |
| `dotnet-iac-gen` | A | قوالب بنية تحتية ككود (Bicep/Terraform) لموارد Azure |
| `dotnet-multi-db-gen` | A | ربط مشروع يتحدث مع أكثر من قاعدة بيانات (SQL Server/PostgreSQL + Oracle للقراءة) |
| `architecture-map-gen` | A | مخططات معمارية حيّة من الكود الفعلي: التبعيات، ERD، C4، التسلسل |
| `speckit-adr` | E | سجل قرار معماري (ADR) بعد كل قرار تقني مهم |

### ٦.٥ المرحلة ٤ — التطوير

**التنسيق:**

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `feature-pipeline` | A | يمشي على خريطة القصص ميزةً ميزة بترتيب التبعيات، ويشغّل سلسلة speckit |

**مسار المواصفات speckit (17):**

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `speckit-analyze` | E | الخطوة الأولى الإلزامية: طلب غامض ← تحليل منظّم + أسئلة مفتوحة |
| `speckit-clarify` | E | يحوّل الأسئلة المفتوحة إلى سجل قرارات، كل سؤال 2–4 خيارات معنونة |
| `speckit-constitution` | E | العقد التقني غير القابل للتفاوض: الكيانات، نقاط النهاية، تغييرات القاعدة |
| `speckit-plan` | E | لوحة مهام مرتّبة بالتبعيات، بعد اختيارك بين 2–3 بدائل خطة |
| `speckit-specify` | E | المواصفة النهائية المكتفية بذاتها — المرجع الذي يتحقّق منه كل ما بعده |
| `speckit-tasks` | E | يزامن جدول المهام إلى `memory-bank/progress.md` كلوحة حيّة |
| `speckit-implement` | E | يولّد كود مهمة **واحدة** فقط، مع فحص قبلي إلزامي |
| `speckit-checklist` | E | بوابة الجودة قبل الالتزام (commit) |
| `speckit-options` | E | كتلة خيارات بمقايضات وتوصية — لأي قرار تقني |
| `speckit-retro` | E | يقارن التقديرات بالواقع بعد شحن الميزة |
| `speckit-taskstoissues` | E | يحوّل لوحة المهام إلى أوامر `gh` جاهزة لإنشاء issues |
| `speckit-git-initialize` | E | يجهّز بنية سير عمل git لمشروع جديد |
| `speckit-git-feature` | E | ينشئ فرع الميزة ويسجّلها كسياق العمل الحالي |
| `speckit-git-validate` | E | مسار تحقّق كامل قبل الدفع (push) عبر كل الطبقات |
| `speckit-git-commit` | E | رسالة commit وفق Conventional Commits من الـ diff المُدرَج |
| `speckit-git-remote` | E | يدفع الفرع ويولّد أمر `gh pr create` كاملاً |
| `work-breakdown` | A | يفكّك أي وحدة عمل (خطأ، إعادة هيكلة، ترحيل) إلى مهام قابلة للإنهاء |

**مولّدات .NET (13):**

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `dotnet-endpoint-gen` | A | نقطة نهاية كاملة جاهزة للإنتاج + DTOs + اختبار تكامل، في تمريرة واحدة |
| `dotnet-migration` | A | ترحيل EF Core بأمان: يتحقّق من انحراف النموذج، ويراجع التغييرات المدمّرة |
| `dotnet-dapper-gen` | A | استعلامات Dapper مُعامَلة للمسارات كثيفة القراءة والتقارير |
| `dotnet-caching-gen` | A | طبقة تخزين مؤقت **مع استراتيجية إبطال صريحة** — يرفض التوليد بدونها |
| `dotnet-messaging-gen` | A | حدث تكامل بنمط outbox — لا نشر fire-and-forget |
| `dotnet-background-job-gen` | A | مهمة خلفية بحراسة idempotency وسياسة إعادة محاولة ومراقبة |
| `dotnet-observability-gen` | A | تسجيل منظّم وتتبّع موزّع ومقاييس، وفق ما هو معلن في `techContext.md` |
| `dotnet-test-gen` | A | ملف اختبار كامل قابل للتشغيل، بنمط يناسب نوع المصدر |
| `dotnet-perf-profile` | C | مراجعة ثابتة لمزالق الأداء + هيكل BenchmarkDotNet للمسار الساخن |
| `dotnet-query-optimizer` | C | مراجعة استعلام LINQ/SQL مع قبل/بعد لخصائص خطة التنفيذ |
| `dotnet-schema-diff` | C | يقارن لقطتَي مخطط (LIVE مقابل TEST) وينتج DDL للمصالحة — للمراجعة لا للتشغيل |
| `dotnet-clean-code-guard` | B | مراجعة شاملة مقابل Clean Architecture وأفضل ممارسات async |
| `dotnet-dependency-audit` | B | حزم مصابة/مهجورة + مسار ترقية آمن مُجزّأ |

**مولّدات React (9):**

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `react-component-gen` | A | مكوّن React كامل جاهز للإنتاج |
| `react-hook-gen` | A | hook مخصّص مكتوب الأنواع مع اختباره |
| `react-api-layer-gen` | A | طبقة API الأمامية كاملة من تعريف نقطة نهاية خلفية |
| `react-state-machine-gen` | A | تدفّق واجهة متعدّد الخطوات كآلة حالة XState صريحة |
| `react-storybook-gen` | A | قصص Storybook تغطي كل تركيبات الـ props ذات المعنى |
| `react-i18n-rtl-gen` | A | تعريب مع معاملة RTL كمواطن من الدرجة الأولى |
| `react-module-federation-gen` | A | إعداد Module Federation لتقسيم micro-frontend |
| `react-test-gen` | A | ملف اختبار كامل لمكوّن أو hook |
| `react-clean-code-guard` | B | مراجعة شاملة لـ React/TypeScript مقابل قواعد المعمارية |
| `react-perf-audit` | B | إعادات الرسم غير الضرورية وفرص الـ memoization |
| `react-accessibility-audit` | B | تدقيق وصولية أعمق من الفحص الأساسي في القاعدة `03` |

**تنفيذ الإصلاحات:**

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `refactor-apply` | A | ينفّذ نتائج مهارات الحراسة — لا تُعِد تشغيل الحرّاس عمياً |

### ٦.٦ فهم كود موجود مسبقاً (4)

الأهم على مستودع موروث، وتُشغَّل **قبل** أي بناء أو تعديل.

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `feature-trace` | B | كيف تعمل هذه الميزة اليوم؟ تتبّع من React إلى الجدول |
| `impact-analysis` | B | ماذا ينكسر إذا غيّرت هذا؟ نطاق الانفجار قبل أي تعديل |
| `feature-inventory` | B | ماذا يفعل هذا النظام أصلاً؟ خريطة القدرات مع نسبة تغطية صادقة |
| `spec-drift-audit` | B | هل ما زال الكود يفعل ما قلنا إنه يفعله؟ |

هذه الأربع تتشارك `.cursor/cache/feature-map.json` — طبقة **سلوكية** فوق
الطبقة البنيوية في `repo-map.json`:

```bash
node .cursor/tools/feature-map.mjs list      # التغطية والحداثة
node .cursor/tools/feature-map.mjs verify    # أي تتبّع أصبح قديماً، ولماذا
```

### ٦.٧ المرحلة ٥ — الاختبار (2 جديدة + الموجود)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `test-strategy` | A | ما الذي يملكه كل مستوى اختبار، والأهم: ما الذي **لا** يملكه |
| `e2e-test-gen` | A | اختبارات متصفح للرحلات الحرجة، مولّدة من معايير القبول بتعليقات `// AC-N:` |
| `load-test-gen` | A | اختبار حمل بعتبات مأخوذة من الـ SLOs الملتزم بها |
| `task-verify` | B | هل المهمة منتهية فعلاً؟ يشغّل أمر التحقّق ويمنع Done بغير دليل |
| `code-review-assistant` | C | مراجعة منظّمة لـ PR أو diff مقابل المواصفة وقواعد المعمارية |
| `compliance-audit` | B | متطلبات SAMA وZATCA وmada — لا يغطّيها فحص أمني عام |
| `api-consistency-audit` | B | اتساق سطح الـ API عبر نقاط نهاية كُتبت في أوقات مختلفة |
| `database-audit` | B | هل طبقة الوصول للبيانات متسقة داخلياً في بيئة واحدة؟ |
| `dependency-upgrade-guard` | B | فحص أمان عابر للطبقات قبل ترقية تبعية مشتركة الأثر |
| `docs-guard` | B | هل كل ادّعاء في وثيقة ما زال مطابقاً للكود؟ |

### ٦.٨ المرحلة ٦ — الإنتاج (2 جديدة + الموجود)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `deployment-pipeline-gen` | A | سير عمل CI/CD يجعل قواعد الحراسة تصمد خارج المحرّر |
| `go-live` | A | دليل التحويل: التسلسل، معايير الإجهاض، التراجع، التحقّق بعد الإطلاق |
| `production-readiness-review` | B | بوابة Go/No-Go عبر سبعة أبعاد — **لا تليّن NO-GO أبداً** |
| `release-safety` | A | كيف يصل التغيير للمستخدمين بلا تحويل شامل: أعلام، حلقات، تراجع |
| `operability-gen` | A | SLIs/SLOs وسياسة ميزانية الخطأ وقواعد تنبيه مرتبطة بدليل تشغيل |
| `devops-audit` | B | تدقيق ما هو قائم: Dockerfiles، workflows، Bicep/Terraform، K8s |
| `postmortem` | A | مراجعة حادث بلا لوم — والمخرج **حارس وقت ترجمة، لا وثيقة** |
| `delivery-metrics` | B | هل التسليم يتحسّن أم يسوء؟ DORA + معدّل إعادة العمل |
| `changelog-gen` | A | مدخل changelog للمستخدم بصيغة Keep a Changelog |
| `release-notes-gen` | A | ملاحظات إصدار بلغة بسيطة بلا تفاصيل تنفيذ |

### ٦.٩ صيانة مساحة العمل — الفئة D (4)

تعمل **بصمت** بلا إعلان.

| المهارة | ماذا تفعل |
|---|---|
| `repo-discovery` | الشرط المسبق الصلب لكل مولّد ومدقّق. يبني `repo-map.json` |
| `context-sync` | يهيّئ أو يحدّث `memory-bank/` كاملاً من الحالة الفعلية للكود |
| `pattern-finder` | يجد الملف الذي هو أفضل مثال قائم لما يوشك أن يُبنى |
| `context-builder` | مجموعة ملفات محدودة بالمهمة، للتغييرات متعدّدة الملفات |

### ٦.١٠ صحّة المنصة نفسها (5)

| المهارة | الفئة | ماذا تفعل |
|---|---|---|
| `platform-health-validator` | C | فحص نظافة شامل لتركيب `cursor-platform` كله |
| `skill-maturity-audit` | B | يدقّق مكتبة المهارات نفسها مقابل عقد مسار التنفيذ المشترك |
| `prompt-quality-audit` | B | يقيّم غموض الطلب والمعايير الناقصة والافتراضات المخفية |
| `technical-debt-tracker` | C | دفتر حيّ للاختصارات المعروفة والتحسينات المؤجّلة |
| `onboarding-doc-gen` | A | يولّد أو يحدّث README و CONTRIBUTING من الحالة الفعلية |
| `enterprise-report-gen` | A | تقرير تنفيذي موحّد بتجميع مخرجات مهارات التدقيق |

---

## 7. الوكلاء الفرعيون الـ14

الوكلاء في `.claude/agents/` **متخصّصون للقراءة فقط**. تفويض العمل إليهم
يبقي آلاف الأسطر المقروءة **خارج المحادثة الرئيسية** — لا يعود إلا الخلاصة.
على مستودع .NET + React كبير، هذا هو الفرق بين إنهاء المهمة ونفاد السياق في
منتصف إعادة الهيكلة.

> **لا تفوّض عملاً يكتب ملفات.** الوكلاء هنا للقراءة بحكم التصميم.
> التوليد يبقى في الخيط الرئيسي حيث تُطبَّق القواعد والخطّافات.

### وكلاء تحليل الكود (8 — أصليون)

| الوكيل | فوّض إليه عندما |
|---|---|
| `feature-analyst` | "كيف يعمل X؟" / "ماذا ينكسر لو غيّرت X؟" — التتبّع الحقيقي يقرأ 30–50 ملفاً |
| `ops-reviewer` | "هل هذا جاهز للشحن؟" — الجاهزية، SLO/التنبيهات، أمان الإطلاق والتراجع. **وهو أيضاً مالك المرحلة ٦** |
| `pattern-scout` | قبل أي مهارة `*-gen` — يجد المثال المحلي القانوني |
| `repo-cartographer` | الخرائط البنيوية، `repo-discovery`، تحديثات `context-sync` |
| `dotnet-auditor` | أي تدقيق .NET يمتدّ لأكثر من ~5 ملفات |
| `react-auditor` | أي تدقيق React/TS يمتدّ لأكثر من ~5 ملفات |
| `security-auditor` | مسوحات الأمان والامتثال ومسار التدقيق |
| `db-auditor` | المخطط، اتساق EF/Dapper، مراجعة الاستعلامات، أمان الترحيل |

### وكلاء دورة الحياة (6 — جدد)

| الوكيل | النموذج | فوّض إليه عندما |
|---|---|---|
| `lifecycle-controller` | sonnet | أي جلسة على مستودع منتج — أي مرحلة، أي بوابات، وهل الطلب ينتمي لمرحلة لاحقة |
| `product-manager` | opus | المرحلة ١ — المتطلبات، الشخصيات، الـ NFRs، خريطة القصص |
| `business-analyst` | opus | المرحلة ٢ — نمذجة مجال جديد. **ليس** `feature-analyst`: ذاك يقرأ كوداً، هذا ينمذج عملاً |
| `solution-architect` | opus | المرحلة ٣ — المعمارية، عقد الـ API، النموذج الفيزيائي، تتبّع الـ NFRs |
| `ux-bridge` | sonnet | المرحلة ٣ للواجهة — جرد الشاشات، الحالات، عقد RTL، رموز التصميم |
| `test-engineer` | sonnet | المرحلة ٥ — استراتيجية الاختبار، وإثبات أن كل معيار قبول له اختبار يؤكّده |

**كل وكيل مرحلة يكتب مرحلته ويحكم على مرحلة أخرى** — لا على مرحلته:

| الوكيل | يكتب | يحكم على |
|---|---|---|
| `product-manager` | المرحلة ١ | بوابة ٥ (هل تُثبت الاختبارات ما طلبناه؟) |
| `business-analyst` | المرحلة ٢ | بوابة ١ |
| `solution-architect` | المرحلة ٣ | بوابة ٢ |
| `ux-bridge` | يشارك في ١ و٣ | لا شيء — لأنه يلمس كلتا البوابتين |
| `test-engineer` | المرحلة ٥ | بوابة ٤ |
| `ops-reviewer` | المرحلة ٦ | لا شيء |
| `security-auditor` | لا شيء | بوابتا ٣ و٦ — ولذلك بالضبط يوقّعهما |

**لماذا opus للمراحل ١–٣ وsonnet للمراحل ٤–٥؟**
المراحل الأولى تتخذ قرارات يكلّف التراجع عنها ستة أشهر. المراحل اللاحقة عمل
حجمي داخل مواصفة، ونمط وجده `pattern-scout`، وستّ قواعد دائمة التفعيل —
القيود تقوم بالتفكير، فاشترِ السرعة.

---

## 8. القواعد الـ12

القواعد في `.cursor/rules/*.mdc`. **أنت لا تستدعيها** — Cursor يحمّلها تلقائياً
حسب نمط الملف. Claude Code لا يحمّل ملفات `.mdc` تلقائياً، لذلك القواعد
العامة مُعادة الصياغة في `CLAUDE.md` ويحقنها خطّاف `SessionStart`.

### دائمة التفعيل (5) — في كل جلسة

| القاعدة | تفرض |
|---|---|
| `00-memory-think` | اقرأ `memory-bank/` وتحقّق من حداثة `repo-map` قبل أي شيء |
| `05-planning-rigor` | لا خطة أو مواصفة بلا جولة استجواب أولاً؛ خيارات بمقايضات دائماً، لا خيار واحد يُقدَّم كأنه الوحيد |
| `09-minimal-changes` | غيّر فقط ما تتطلّبه المهمة — لا إعادة تنسيق ولا توسّع نطاق |
| `10-evidence-and-dependency-guard` | تأكّد أن الأصناف والجداول والحزم **موجودة** قبل الإشارة إليها. لا حزمة جديدة إلا بطلب صريح |
| `11-lifecycle-gate` | لا تشغّل عملاً يخصّ مرحلة لاحقة؛ لا تعتمد بوابة نيابة عن المستخدم |

**عدد الأسئلة في `05-planning-rigor` مُقنَّن:**

| نطاق الميزة | الحد الأدنى للأسئلة |
|---|---|
| صغيرة، ≤ 3 ملفات، بلا مصادقة/مدفوعات | 5 |
| ميزة قياسية، أي طبقة | 7 |
| ميزة كاملة (خلفية + واجهة) | 10 |
| مصادقة، مدفوعات، ترحيل بيانات، عقد عابر للخدمات | 10 |
| تغيير متعدّد الخدمات أو كاسر لعقد API | 12 |

### مرتبطة بأنماط الملفات (7)

| القاعدة | تنشط على | تفرض |
|---|---|---|
| `01-specify-rules` | `specs/**`, `src/**`, `frontend/**` | لا تنفيذ بلا مواصفة؛ مسار speckit تسلسلي إلزامي |
| `02-dotnet-architecture-guard` | `*.cs`, `*.csproj` | حدود طبقات Clean Architecture — المعالِجات لا تستخدم `DbContext` مباشرة |
| `03-react-architecture-guard` | `*.tsx`, `*.ts` | معمارية المكوّنات — لا `fetch` داخل مكوّن؛ طبقة API + React Query |
| `04-security-guard` | كل المصادر والإعدادات وملفات CI | OWASP، الأسرار، الحقن. لا JWT في `localStorage`؛ SQL مُعامَل |
| `06-database-provider-guard` | `*.cs`, `*.sql` | انضباط SQL متعدّد المزوّدين — لهجة صحيحة لكل مزوّد |
| `07-audit-trail-guard` | `*.cs` | مسار تدقيق (من/متى/ماذا تغيّر) + ثوابت المجال المالي. `decimal` للنقود |
| `08-rtl-i18n-guard` | `*.tsx`, `*.css` | خصائص CSS منطقية؛ تنسيق أرقام وتواريخ حسب اللغة |

### غير قابل للتفاوض

- تأكّد من وجود الأصناف والواجهات والجداول والحزم ومفاتيح الإعداد قبل الإشارة إليها.
- **.NET:** Clean Architecture — المعالِجات لا تلمس `DbContext` مطلقاً.
- **React:** لا `fetch` في المكوّنات؛ طبقة API + React Query.
- **الأمان:** لا أسرار في المصدر؛ لا JWT في `localStorage`؛ SQL مُعامَل دائماً.
- **التعديلات المالية:** سجّل من/متى/ماذا تغيّر؛ استخدم `decimal` للنقود.
- **واجهة عربية/RTL:** خصائص CSS منطقية؛ تنسيق واعٍ باللغة.
- **قواعد بيانات متعدّدة:** `DbContext` واحد لكل مصدر بيانات؛ لهجة SQL صحيحة لكل مزوّد.
- **دورة الحياة:** لا تشغّل مهارة مرحلة لاحقة؛ لا تعتمد بوابة نيابة عن المستخدم.

---

## 9. الخطّافات الـ7 — ما يُنفَّذ ميكانيكياً

القواعد نصائح؛ الخطّافات ليست كذلك. الربط في `.claude/settings.json`.
كلها Node خالص بلا أي تبعيات، وتعمل على Windows وmacOS وLinux.

| الحدث | الملف | الأثر |
|---|---|---|
| `SessionStart` | `session-start.mjs` | يحقن مرحلة دورة الحياة الحالية + خلاصة الطبقة الأولى من `memory-bank` + حداثة `repo-map.json`. يجعل القاعدتين `00` و`11` تلقائيتين |
| `PreToolUse` (كتابة) | `guard-write.mjs` | **يمنع** تعديل `repo-map.json` و`feature-map.json` و`lifecycle/state.json` يدوياً، والكتابة في ملفات الأسرار، وكلمات المرور المضمّنة في النص |
| `PreToolUse` (كتابة) | `guard-phase.mjs` | **يمنع** كل كتابة تحت `src/` و`backend/` و`frontend/` قبل اعتماد بوابة التصميم |
| `PreToolUse` (Bash) | `guard-bash.mjs` | **يمنع** `dotnet add package` و`npm install <pkg>` و`git push --force` و`ef database update` على اتصال غير محلي |
| `PreToolUse` (`mcp__.*`) / `beforeMCPExecution` | `guard-mcp.mjs` | **يمنع** ما يخالف `.cursor/mcp-policy.json`: كتابة على خادم للقراءة فقط، أو أداة في قائمة المنع (`merge_*`, `delete_*`, `*force*`)، أو عبارة SQL غير `SELECT`، أو عبارة ثانية بعد `;`، أو أداة خارج مرحلتها المسموحة |
| `PostToolUse` (كتابة) | `post-edit-verify.mjs` | يشغّل `dotnet format` / `eslint --fix` على الملف المعدَّل ويعيد الأخطاء |
| `Stop` | `stop-memory-check.mjs` | ينبّه إذا تغيّر المصدر ولم يُحدَّث `memory-bank/activeContext.md` |

بالإضافة إلى ملفَّين ليسا خطّافات:
`_lib.mjs` (دوال مشتركة: `block()`, `ok()`, `inject()`) و`sync-skills.mjs`
(مولّد الـ shims).

### القاعدة الأهم عند التعامل مع خطّاف

> **إذا منعك خطّاف، توقّف وأخبر المستخدم بالسبب.**
> لا تلتفّ حوله بأداة أخرى. المنع إشارة للسؤال، لا مشكلة تُحَل.

### سياسة MCP — القفل الثالث

`.mcp.json` عندك فيه قفلان أمنيان بالفعل، وكلاهما مِلك جهة أخرى: ترويسة
`X-MCP-Readonly` على خادم GitHub، و`--access-mode=restricted` على
postgres-mcp. والتعليق في الملف نفسه يقولها صراحةً: **«وضع الوصول دفاع في
العمق، لا الحدّ»**. أسقِط ترويسة، أو وجّه `DATABASE_URI` إلى دور بصلاحيات
أوسع، أو أضِف خادماً يوم خميس — ويزول القفل دون أن يعترض شيء في المستودع.

`guard-mcp.mjs` هو القفل الذي يعيش مع الكود. يقرأ `.cursor/mcp-policy.json`
ويطبّق أربع طبقات:

| الطبقة | تمنع |
|---|---|
| قائمة المنع | `merge_*`, `delete_*`, `*force*` — تُمنع **حتى لو** ضُبط `write:true`، لأنها العمليات التي لا تُلغيها مراجعة لاحقة |
| راية الكتابة | أي أداة اسمها يبدأ بـ create/update/delete/push/… على خادم `write:false` |
| فِعل الـ SQL | العبارة يجب أن تبدأ بـ `SELECT`/`WITH`/`EXPLAIN`… — والأهم: **عبارة ثانية بعد `;` تُمنع**، لأن `SELECT` في البداية لا تقول شيئاً عمّا بعد الفاصلة المنقوطة |
| قيد المرحلة | أداة تُستخدم خارج مراحل دورة الحياة المسموح بها لها |

خادم غير مذكور في السياسة **يُسمح ويُبلَّغ عنه**، لا يُمنع. المنع الافتراضي
يبدو أصرم وهو أسوأ: أول خادم غير مذكور يعطّل مساحة العمل، فيُطفئ أحدهم
الخطّاف، فيصير كل الخوادم غير مذكورة.

### مخارج الطوارئ الموثّقة

| المتغيّر | يتجاوز | متى |
|---|---|---|
| `CLAUDE_ALLOW_TIER2_EDIT=1` | منع الكتابة في الطبقة الثانية من `memory-bank` | عند تنفيذ ترقية وافق عليها المستخدم |
| `LIFECYCLE_OVERRIDE=1` | بوابة التصميم في `guard-phase.mjs` | استكشاف مقصود (spike)، يضبطه إنسان |

كلاهما يُضبط **عن قصد من إنسان**. اقتراح الوكيل لاستخدامهما مخالفة.

---

## 10. الأدوات الـ18

في `.cursor/tools/`. كلها Node خالص بلا تبعيات، وتُشغَّل يدوياً أو من CI.
هذه هي الطبقة التي تعطي إجابات **محسوبة** بدل إجابات مُخمَّنة.

### `lifecycle.mjs` — آلة حالة دورة الحياة

```bash
node .cursor/tools/lifecycle.mjs init --name "المنتج" [--existing]
node .cursor/tools/lifecycle.mjs status [--json]
node .cursor/tools/lifecycle.mjs check [PHASE] [--json]
node .cursor/tools/lifecycle.mjs approve PHASE --by "اسم" [--note "..."]
node .cursor/tools/lifecycle.mjs advance
node .cursor/tools/lifecycle.mjs rollback PHASE --reason "..."
node .cursor/tools/lifecycle.mjs gate [PHASE]
```

يفحص وجود المُخرَجات **وأنها ليست قوالب فارغة** (يكتشف علامات `EXAMPLE`/`TODO`
ونِسَب الخانات `[...]` العالية). يرفض `approve` عند نقص مُخرَج.

### `feature-map.mjs` — الطبقة السلوكية

```bash
node .cursor/tools/feature-map.mjs list            # التغطية والحداثة
node .cursor/tools/feature-map.mjs verify          # ما الذي أصبح قديماً ولماذا
node .cursor/tools/feature-map.mjs upsert /tmp/x.json
```

كل تتبّع يسجّل بصمة محتوى كل ملف غطّاه، فالحداثة **محسوبة لا مخمَّنة**.

### `task-graph.mjs` — حجم المهمة مفروض لا مقدَّر

```bash
node .cursor/tools/task-graph.mjs validate <plan.md>   # ≤8 ملفات، ≤2 طبقة، أمر تحقّق، بلا دورات
node .cursor/tools/task-graph.mjs graph    <plan.md>   # الدفعات المتوازية والمسار الحرج
node .cursor/tools/task-graph.mjs next     <plan.md> --done T-01
node .cursor/tools/task-graph.mjs split    <plan.md> T-04
```

> **لا تعرض أبداً لوحة فشلت في `validate`** — أصلحها وأعد التشغيل أولاً.

المنطق: S/M/L تقول كم يستغرق العمل، لكنها لا تقول شيئاً عن إمكانية إنهائه في
جلسة واحدة. مهمة تلمس 25 ملفاً لا تأخذ وقتاً أطول — بل **تفشل في المنتصف
وتترك تغييراً نصف مُطبَّق**.

### `ac-trace.mjs` — الجسر بين معايير القبول والاختبارات

```bash
node .cursor/tools/ac-trace.mjs check specs/features/<slug>.md   # الفجوات
node .cursor/tools/ac-trace.mjs matrix specs/features/<slug>.md  # مصفوفة التتبّع
node .cursor/tools/ac-trace.mjs lint                             # اختبارات فارغة أو ضعيفة
```

يقرأ تعليقات `// AC-N:` التي يصدرها مولّدا الاختبارات، ويفشل على: معيار قبول
لا يدّعيه أي اختبار، اختبار يدّعي معياراً حذفته المواصفة، معيار اختباره
الوحيد مُتجاوَز، اختبار مدّعٍ بلا تأكيد، وتأكيد **لا يمكن أن يفشل**.

هذه الأداة هي التي تُغلق الحلقة من المرحلة ٥ إلى المرحلة ١.

### `delivery-metrics.mjs` — هل نتحسّن؟

```bash
node .cursor/tools/delivery-metrics.mjs report --days 90    # DORA + إعادة العمل
node .cursor/tools/delivery-metrics.mjs trend  --days 180   # اتجاه السير
```

يُعلّم كل مقياس بـ MEASURED أو PROXY — لا يدّعي دقة لا يملكها.

> **لماذا يهمّ هذا هنا تحديداً:** أبحاث DORA لعامَي 2025/2026 وجدت أن تبنّي
> الذكاء الاصطناعي يرفع الإنتاجية 2–18% بينما يتدهور الاستقرار — معدّل فشل
> التغيير من 8% إلى 14% في إحدى الدراسات، وحجم الـ PR +154%، ووقت المراجعة
> +91%. منصة من 96 مهارة **مُضخِّم إنتاجية**، وهي بالضبط ذلك التكوين.
> شغّل `/delivery-metrics` بدل افتراض أي اتجاه تسير فيه.

### `artifact-schema.mjs` — مصفوفة التتبّع

```bash
node .cursor/tools/artifact-schema.mjs check      # معلّق / مكرّر / في غير موضعه / سلسلة منقطعة
node .cursor/tools/artifact-schema.mjs trace S-07 # معرّف واحد: صعوداً ونزولاً
node .cursor/tools/artifact-schema.mjs graph      # كل المعرّفات وأين تنقطع سلاسلها
node .cursor/tools/artifact-schema.mjs validate   # الـ front-matter مقابل schemas/
```

`ac-trace` يغطّي نصف السلسلة: معيار القبول ← الاختبار. وهذه تغطّي النصف الآخر:

```
FR ← ── S ── → UC ── → EP, SC          NFR ── → ADR, EP
```

**لماذا لم أجعل Markdown مُولَّداً من JSON.** القراءة البديهية لـ«أضف schemas»
هي: اكتب المتطلبات JSON وارسم منها Markdown. وهذا خطأ هنا: قيمة المنصة أن
وكيلاً يكتب هذه المستندات **نثراً** بخيارات ومقايضات وتعليل، وPRD مضغوط في
كائن JSON يفقد بالضبط الجزء الذي يستحق القراءة — والـ96 مهارة كلها تنتج
Markdown.

فالنمط المتَّبع هو ما تفعله `ac-trace` عندك أصلاً وينجح: **وسم مقروء آلياً
داخل مستند مكتوب للبشر**. وهو هنا شيئان:

1. **front-matter** — ما هو هذا المستند وممّ اشتُقّ، يُتحقَّق منه مقابل
   `schemas/document.schema.json`
2. **المعرّفات في النثر** — `FR-12`, `S-07`, `UC-04`, `EP-05`، وهي أصلاً ما
   تُصدره مهارات دورة الحياة، تُقرأ الآن كرسم بياني

لا شيء يحتاج إعادة كتابة. الرسم يعمل على المستندات الموجودة اليوم.

> والبادئات **لم تُخترَع هنا**: `UC` `INV` `AC` `BR` `S` `EP` `SC` `R` `FR` هي
> ما تُصدره المهارات فعلاً وما يقرؤه `ac-trace.mjs` أصلاً. تقنين القائم لا
> يكلّف شيئاً؛ اختراع `REQ-001` كان سيعني إعادة كتابة كل مهارة وكل مثال بلا مكسب.

**والأهم:** `lifecycle.mjs check` صار يُفشل على سلسلة منقطعة. يعني معايير
بوابات مثل «كل قصة تتبع حالة استخدام» انتقلت من **حكم يقرؤه مراجع** إلى
**شهادة ميكانيكية لا تُجادَل**.

### `change-request.mjs` — التغيير بنطاق أثر محسوب

```bash
node .cursor/tools/change-request.mjs impact BR-4       # تنبّؤ بلا تسجيل
node .cursor/tools/change-request.mjs open --changes "BR-4" --reason "..." --by "اسمك" --risk HIGH
node .cursor/tools/change-request.mjs list
node .cursor/tools/change-request.mjs close CR-0001 --note "..."
```

`rollback` يفتح مرحلة ويصفّر كل ما بعدها — الأداة الصحيحة لـ«المعمارية
خاطئة»، والخاطئة تماماً لما يحدث فعلاً: العميل غيّر قاعدة دفع واحدة، وعلى
أحدهم أن يعرف ماذا تمسّ. `rollback` يجيب «كل شيء بعد التصميم» — صحيح
وعديم الفائدة. أما هذه فتمشي على رسم المعرّفات وتجيب بدقّة:

```
FR-2 ──→ S-2 ──→ UC-2 ──→ EP-2
```

**وتتبع الاستشهاد لا عقد الاكتمال.** حقل `tracesTo` عقدُ اكتمال يفحصه
`check`؛ استخدامه هنا يجيب عن السؤال الخطأ: `BR-1` بلا `tracesTo` لأن قاعدة
العمل لا تدين بشيء لما بعدها، لكن تغييرها يمسّ بداهةً كل حالة استخدام
تفرضها. والانحياز مقصود: نطاق أثر واسع يكلّف مراجعاً بعض القراءة، وضيّق
يشحن تناقضاً.

**ولا تُبطِل شيئاً.** المرحلة تصير `STALE` لأن بصمات مُخرَجاتها تحرّكت، وهو
ما يشتقّه `lifecycle.mjs` وحده. مسار إبطال ثانٍ أضعف كان سيختلف مع الأول.
فهي **تنبّؤ وسِجِل**، والبصمات هي الإنفاذ.

### Definition of Ready — هل تدخل هذه القصة التطوير؟

```bash
node .cursor/tools/artifact-schema.mjs ready S-7    # قصة واحدة
node .cursor/tools/artifact-schema.mjs ready        # كل القصص
```

`/task-verify` عندك يرفض وسم مهمة Done بلا دليل — أي أن **Definition of Done**
محروسة. ولم يكن أحد يسأل السؤال في الطرف الآخر، وهناك يقع الخطأ الأرخص: قصة
تدخل التطوير نصف مواصَفة تكلّف إعادة كتابة، وقصة تسقط في هذا الفحص تكلّف فقرة.

يحسب ثمانية بنود من العشرة من الرسم البياني: المتطلب، معايير القبول، حالة
الاستخدام، قواعد العمل، نقطة النهاية، الشاشة، تصميم الأمان، تصميم البيانات.

والبندان اللذان **لا يستطيع** حسابهما — ترتيب التبعيات وحجم المهام، وكلاهما
لـ`task-graph.mjs` — يُبلَّغ عنهما كبنود يدوية لا تُحذف بصمت. قائمة تحذف ما
تعجز عنه هي كيف تتوقّف بوابة عن الحراسة.

> `~` على بند الواجهة تحذير لا إخفاق: ليس لكل قصة شاشة. لكن تأكّد أنها فعلاً
> قصة خلفية، لا شاشة لم يصمّمها أحد.

و`/feature-pipeline` يرفض بدء قصة غير جاهزة.

### `self-audit.mjs` — هل كلُّ ضابطٍ تدّعيه المنصّة موصولٌ فعلاً؟

**كُتِبت لعيبٍ وقع بالفعل.** `guard-phase.mjs` كُتِب واختُبِر ووُثِّق ونُسِخ إلى
الملحق القابل للتوزيع — **ولم يُوصَل قط بتوصيلة الخطّافات المولَّدة فيه**. فلكل
من ثبّت الملحق بدل استنساخ المستودع، **بوابة التصميم لم تمنع شيئاً على
الإطلاق**، وكل وثيقة كانت تقول إنها تمنع.

هذا أخطر صنف من العيوب هنا: **ضابطٌ موجود لكنه غير قابل للوصول**. وهو أسوأ من
ضابطٍ ناقص، لأن **الناقص يُلاحَظ والمفصول يُوثَق به** — والمنصّة كلّها قائمة على
أن الشهادة الميكانيكية هي التي لا تُجادَل.

```bash
node .cursor/tools/self-audit.mjs run      # التوصيل + الفاحصات الثلاث القائمة
node .cursor/tools/self-audit.mjs wiring   # التوصيل فقط
```

**ولا تكرّر شيئاً:** `docs-lint.mjs` يملك الروابط والإشارات الشبحية،
و`platform-metadata.mjs` يملك الأعداد، و`build-plugin.mjs check` يملك شجرة
الملحق — و`run` يستدعي الثلاثة فيصير الأمر واحداً. ما تضيفه هو **التوصيل** فقط:

| # | ما يفحصه |
|---|---|
| A1 | كل سكربت خطّاف موصول في `.claude/settings.json` **و** `.cursor/hooks.json` |
| A2 | كل أمر موصول يشير إلى ملف موجود |
| A3 | **المضيفان يوصلان المجموعة نفسها** |
| A4 | الملحق المبنيّ يوصلها أيضاً — العيب الأصلي |
| A5 | مُراجِع كل بوابة وكيلٌ حقيقي، وليس أبداً أحد كُتّابها |
| A6 | لا أداة يتيمة: شيءٌ بُني ثم لم يُوصَل بشيء |
| A7 | أن هذا التدقيق نفسه يعمل في CI |

**والفحص A3 كان مكتوباً كطلبٍ في تعليق `.cursor/hooks.json` نفسه:** «نسختان من
حارس، واحدة لكل محرّر، هي الطريقة التي تتوقّف بها إحداهما عن الإنفاذ بصمت» — ولم
يكن شيء يتحقّق من اتّفاقهما. حارسٌ يعمل في محرّر ولا يعمل في الآخر **غير مرئي من
داخل أيٍّ منهما**.

**وA7 ليس ظرافةً:** تدقيقٌ لا يشغّله أحد هو **بالضبط** العيب الذي وُجد ليجده،
وشحنُه مفصولاً كان سيكون نكتةً رديئة.

> **وأول ما وجدته حين شغّلتها:** أنّ **بوابة ٤ فقدت إعلان مُراجِعها**. في
> التعديل الرابع عشر نسختُ نسخةً قديمة من الملف فوق النسخة المعدّلة، فضاع
> `Reviewed by: test-engineer` وقسمُ «من يراجع هذا ولماذا» — أي أن `record-gate`
> كان سيقبل حكماً من أي أحد، **بما فيهم كاتب الكود**. الأداة أمسكت انحداراً
> أدخلتُه أنا قبل تعديلين. هذا أفضل دليل على أنها ليست تمثيلاً.

---

### `delivery-intel.mjs` — هل تُنتِج العملية شيئاً، أم تُؤدَّى تمثيلاً؟

`delivery-metrics.mjs` يقرأ **git** ويعيد DORA: تواتر النشر، زمن الريادة، معدّل
فشل التغيير، إعادة العمل. ذلك سؤال الصناعة عن قاعدة الكود، وهو مُجاب هناك.

**وهذه تقرأ سجلّات دورة الحياة نفسها** — أحكام البوابات وعدد محاولاتها،
والتجاوزات وهل انقضت، وسجلّات الإصدار ومن وقّعها، والحوادث وكيف اكتُشفت، وخطّ
أساس الدَّين المعماري وهل تحرّك يوماً — وكلّها كانت تُقرأ **ملفاً ملفاً**. وحين
تتقاطع تجيب عمّا لا يجيبه أيٌّ منها وحده.

```bash
node .cursor/tools/delivery-intel.mjs report
node .cursor/tools/delivery-intel.mjs questions
```

**ليست لوحة نتائج.** الرقم الذي يُعرَض على أنه «جيّد» يُحسَّن. طباعة «نسبة نجاح
البوابات ١٠٠٪» بعلامة صحّ هي **تعليمة بأن تتوقّف عن إفشال البوابات**، وأرخص
طريقة لتحقيقها أن تكفّ عن النظر. فلا شيء هنا مُقيَّم أو مُرتَّب أو له هدف. كل
ملاحظة تُطبع بـ**القراءتين** اللتين تحتملهما وبالدليل الذي يفرّق بينهما:

```
٨ أحكام بوابات مسجّلة، ولا NO-GO واحد.
    إمّا   كان العمل جيّداً بما يكفي في كل مرّة
    أو     لم تكن المراجعة يوماً في موضع يقول «لا»
    ما يفرّق: هل سُجِّل حكمٌ قبل اكتمال مُخرَجاته، وكم استغرقت كل بوابة؟
             مراجعةٌ تستغرق ساعة وتنجح دائماً هي القراءة الثانية.
```

**والأعداد الصغيرة تُقال بصوت عالٍ.** إصداران وحادثة واحدة **لا تسند أي
استنتاج**، وأداةٌ ترسم خطّ اتجاه عبرها أسوأ من أداةٍ تصمت. كل قسم يطبع الـn
خاصّته، ويرفض لغة «الاتجاه» تحت خمس نقاط.

**وأطرف ما في الأمر:** شغّلتها على نموذج الاختبار الذي بنيتُه بسكربت يعتمد كل
بوابة آلياً — فقالت لي بالضبط ذلك: ثمانية أحكام بلا رفض واحد، وستّ بوابات
اعتُمِدت في **نفس يوم** تسجيل الحكم، وسجلّ إصدار قُطِع ولم يُوقَّع. الأداة شخّصت
النموذج الذي وُلدت فيه.

---

### `incidents.mjs` — ما علّمه الإنتاج، وهل ما زال الحارس واقفاً

مهارة `/postmortem` جيّدة أصلاً: سُلَّم تصعيد من ثماني درجات، وإصرارٌ على أن
يصير الاكتشاف مدخلاً في `BannedSymbols.txt` أو اختبارَ اتّفاقية لا فقرةً. وفي
خطوتها السادسة تكتب — بكلماتها — العطبَ الذي لم يكن في المستودع ما يمسكه:

> «الفِرَق تحذف دفاعات نافعة أثناء التنظيف **لأن لا أحد سجّل أنها أفادت**.»

**ولم يسجّل أحد.** لم تكن هناك صلة بين حادثة والحارس الذي اشترته، فحدثت ثلاثة
أشياء بصمت وكثيراً:

1. بندُ العمل في الـpostmortem أُغلِق دون أن يُبنى الحارس أصلاً؛
2. الحارس بُني ثم أزاله بعد ستّة أشهر من لا يعرف لماذا كان هناك — المحلِّل
   يُكتَم، واختبار الاتّفاقية يُعلَّم `Skip`، والرمز الممنوع يُحذف في تنظيف؛
3. تكرّرت الحادثة نفسها ولم يربط أحدٌ بينهما.

والثلاثة **قابلة للكشف آلياً** لحظةَ أن تسمّي الحادثةُ حارسَها بالمسار.

```bash
node .cursor/tools/incidents.mjs open --title "..." --detected reconciliation \
     --guard "templates/dotnet/BannedSymbols.txt#Retry.Helper.Retry" \
     --guard "tests/Arch/Idempotency.cs#Retriable_Operations_Are_Idempotent" \
     --falsifies "NFR-3,AC-12" --by "اسمك"
node .cursor/tools/incidents.mjs check      # بوابة ٦ و CI
node .cursor/tools/incidents.mjs learned    # ماذا كذّب الإنتاج
```

**ثلاث حالات للحارس، وأسوأها الثالثة:** `MISSING` (اختفى)، `COMMENTED` (بقي في
سطر معلَّق فقط)، و`SKIPPED` — موجودٌ لكنه داخل اختبار عُلِّم بالتخطّي. الثالثة
هي الأسوأ لأن **السجلّ ما زال يقول إن الحارس هناك**.

**والدرجة تُصنَّف من مكان الحارس.** إن كانت أقوى درجة هي ٧ أو ٨، تقول الأداة
ذلك: هذه حالة «الدرجة الثامنة مرتديةً قبّعة» التي تسمّيها المهارة نفسها. وإن كان
الاكتشاف لا يقبل المَكْنَنة حقاً، فـ`--unmechanisable "السبب"` يسجّل ذلك
**بالكلمات** — وهو شيء مختلف عن الاستسلام بصمت، والسجلّ يفرّق بينهما.

**والتكرار يُسأل عنه لحظةَ الفتح**، لأن لا أحد يسأله لاحقاً: حادثةٌ تسمّي حارساً
سمّته حادثةٌ أخرى تطبع التداخل فوراً. حارسٌ كان يُفترَض أن يمنع هذا ولم يمنعه:
إمّا لم يُبنَ قط، أو أُزيل، أو لا يغطّي هذه الحالة — **وثلاثتها postmortem
مختلفة**، وعليك أن تعرف أيّها.

**والنصف الآخر: ماذا كذّب الإنتاج.** `nfr.md` يقول P99 دون ٤٠٠ مللي ثانية،
والإنتاج قال ١٢٠٠ لستّ ساعات. الـNFR ليس «في خطر» — هو **خاطئ**، ووثيقة المرحلة
الأولى ما زالت تؤكّده. `--falsifies NFR-3` يسجّل ذلك، و`learned` يضعه أمام
مراجِعَي البوابة ١ والبوابة ٥ — **وهو شيء لا يمكن أن يعرفه المراجِع من الوثيقة
نفسها أبداً**.

> **وما لا تفعله:** لا تقيس شيئاً. لا ابتلاع تنبيهات، ولا حساب استهلاك SLO، ولا
> كشفَ حوادث — تلك تعيش في المراقبة التي يشغّلها المنتج فعلاً، وأداةٌ في مستودع
> تتظاهر بمعرفتها إنما تخترع أرقاماً. هذه تسجّل ما تعلّمه إنسان، **ثم تُلزِم
> المستودع به**.

---

### `fitness.mjs` — المعمارية كتأكيد، لا كفقرة

البوابة الثالثة تنتهي بخطوة ترقية، وتقول عنها حرفياً:

> «هذه الترقية هي ما يجعل التصميم **مُلزِماً**. متى صار التصنيف الطبقي في
> `architecture.md`، فرَضَته القاعدة ٠٢ على كل ملف `.cs` لبقية عمر المشروع.
> تخطَّها فيبقى التصميم وثيقة، ونفِّذها فيصير **خطأ ترجمة**.»

**ولم يكن هذا صحيحاً.** القاعدة ٠٢ ملفُّ نثرٍ يقرؤه الوكيل ويختار الامتثال له —
أي احتمالي — و`dotnet-auditor` يمسح حين يتذكّر أحدٌ أن يطلب — أي عَرَضي. **ولا
شيء في المستودع كان يؤكّد يوماً أن Domain لا يشير إلى Infrastructure**، وهي
عمليةُ grep واحدة على تعليمات `using` وعناصر `<ProjectReference>`، وهي الادّعاء
الحامل للمعمارية كلها.

```bash
node .cursor/tools/fitness.mjs rules              # ما اشتُقّ، ومن أين
node .cursor/tools/fitness.mjs check              # بوابة ٤ و CI: الجديد فقط
node .cursor/tools/fitness.mjs baseline --accept
```

**من أين تأتي القواعد؟ لا من ملف إعدادات.** ملفُّ `fitness.json` سيكون **مصدر
حقيقة معماري رابع** إلى جانب `memory-bank/architecture.md` والقاعدة ٠٢
والـADRs، والأربعة ستتناقض خلال شهر. القواعد **تُقرَأ من**
`memory-bank/architecture.md` — فكتلة التصنيف الطبقي فيه تسمّي كل طبقة وما يجوز
لها أن تعتمد عليه، وكتلة الواجهة تقول إن `shared/` لا يجوز أن يستورد من
`features/`. وذلك الملف من الطبقة الثانية: يكتبه إنسان، ويُرقّى عند البوابة ٣،
ويمنع `guard-write.mjs` الوكلاء من الكتابة فيه. **فترقية التصميم هي ما يوجِد هذه
الفحوص.**

وإن كان الملف ما زال قالباً، تقول الأداة ذلك ولا تفحص شيئاً: معماريةٌ لم تُرقَّ
ليست مخالفة تُبلَّغ، بل بوابةٌ تُخطِّيت — واختراع قواعد لم يوافق عليها أحد أسوأ
من ألّا تفحص شيئاً.

**ستّ دوال لياقة:** اتجاه الطبقات (على `using` و`<ProjectReference>`، **بالإغلاق
التعدّي** حتى لا يكون استخدام Infrastructure لـDomain إيجابيةً كاذبة)، ونقاء
Domain من حزم البنية التحتية، وحدود الواجهة، ودورات الاستيراد، ووجود مشروع
اختبارات المعمارية الذي يدّعي الملف أنه بوابة البناء.

**والـbaseline مِسنَنة (ratchet)، وهي ما يجعل هذا قابلاً للحياة.** وجِّه فاحصاً
جديداً إلى قاعدة كود قائمة فيعيد أربعمئة مخالفة، ويُطفَأ بعد ظهر ذلك اليوم. دالّة
اللياقة يجب أن تكون **تراكمية** لتكون مستمرّة: `check` يفشل على **الجديد فقط**
منذ `lifecycle/fitness-baseline.json`. العدد ينزل ولا يصعد أبداً، و**استبدال
مخالفة مُصلَحة بأخرى جديدة مرفوض** حتى لو بقي العدد ثابتاً. وهذا ليس `--force`:
إنه سجلٌّ مؤرَّخ لدَينٍ معماري، والأداة تطبع عمره — لأن baseline لم يتحرّك منذ
سنة هو فحصٌ مُعطَّل يرتدي قبّعة.

**وعيبان أمسكهما الاختبار قبل التسليم:** المِسنَنة كانت تقارن **الأعداد** فقط،
فإصلاح مخالفة وإدخال أخرى يمرّ بصمت — وهو تحديداً التآكل الهادئ الذي وُجدت
الأداة لمنعه؛ صارت تقارن المجموعات. والثاني أخطر: بعد ذلك الإصلاح صار **إنشاء
الـbaseline الأول مستحيلاً**، لأن كل مخالفة «جديدة» حين لا يوجد ما تُقاس عليه —
فالمِسنَنة لم تكن لتبدأ أبداً.

---

### `failure-modes.mjs` — ماذا يحدث حين يفشل ما لا تتحكّم به

كل اختبار أضافه `risk-profile.mjs` يسأل عن سلوك **هذا** النظام أمام مُدخَل سيّئ.
ولا شيء في المنصّة كلها كان يسأل السؤال الآخر: ماذا يحدث حين تتوقّف بوّابة mada
عن الردّ، أو ترجع ZATCA خطأ 503 أربع ساعات، أو تُعيد الطابور تسليم رسالة سبق أن
حرّكت مالاً؟

**هذه هي الأعطال التي تُسقط أنظمة الدفع فعلاً.** تُصمَّم في المرحلة ٣ أو لا
تُصمَّم أبداً، ولا تُختبَر تقريباً، ويكتشفها من هو تحت الطلب في الثالثة فجراً.

```bash
node .cursor/tools/failure-modes.mjs scan
node .cursor/tools/failure-modes.mjs check          # بوابتا ٣ و٦
node .cursor/tools/failure-modes.mjs explain MadaPaymentClient
```

**الذي يقتل الخدمات فعلاً: مهلة مفقودة.** التبعية التي **تفشل** يمكن احتمالها —
النداء يعود بخطأ تعالجه. التبعية التي **تبطئ** هي التي تأخذ الخدمة معها: الطلبات
تتكدّس على تجمّع خيوط لا يُفرَّغ أبداً. و`new HttpClient()` ينتظر **مئة ثانية**
افتراضياً، وهو تحت أي حِمل حقيقي انقطاعٌ من صنع يدك. لذلك يُبلَّغ عنه كمانع.

**وحدة التحليل موضع التسجيل لا الملف** — `AddHttpClient` و`AddDbContext`
و`AddMassTransit` و`axios.create` — لأن هناك تُضبَط المرونة. البحث في الملف كلّه
عن كلمة `Timeout` يجد واحدة تخصّ شيئاً آخر ويُبلغ عن حماية غير موجودة، وهذا هو
**الاتجاه الخطر** للخطأ. و`AddStandardResilienceHandler` مفهوم صراحةً: يضبط
المهلة وإعادة المحاولة وقاطع الدائرة معاً، وأداةٌ تقول «لا مهلة» عن عميل .NET 8
مبنيّ بشكل صحيح تُهمَل خلال أسبوع — عن حقّ.

| المستوى | ماذا يعني |
|---|---|
| `BLOCKING` | لا مهلة؛ أو إعادة محاولة بلا idempotency على مسار مال |
| `HIGH` | إعادة محاولة بلا قاطع دائرة؛ لا اختبار يُفشل التبعية |
| `MED` | سلوك الفشل غير مقرَّر في التصميم؛ لا مهلة أمر لقاعدة البيانات |

**سُلَّم الفوضى — وأين يتوقّف بصدق:**

| # | المستوى | أين يُنفَّذ |
|---|---|---|
| ١ | مُصمَّم — لكل تبعية سلوك فشل مقرَّر | بوابة ٣ |
| ٢ | مبنيّ — مهلة، إعادة، قاطع، idempotency | هذه الأداة |
| ٣ | مُختبَر — اختبار يُحاكي فشل التبعية | هذه الأداة |
| ٤ | **مُتمرَّن عليه** — أحدهم كسرها في staging وراقب | بوابة ٦ |
| ٥ | تجارب فوضى على حركة مرور حيّة | **خارج النطاق** |

المستويات ١–٤ حقيقية ورخيصة وآمنة. أمّا الخامس فليس مقترحاً هنا ولن تولّده
الأداة: حقنُ الأعطال في نظام دفع خاضع لإشراف ساما **تغييرٌ رسمي** له مالك مُسمّى
وسجلّ يواجه الجهة التنظيمية، ولا أداة في مستودع تملك صلاحية الإذن به. مهارةٌ
تُخرِج سكربت فوضى إنتاجي إنما تُولّد شيئاً لا يُسمح لأحد بتشغيله — نموذجاً، لا
ضابطاً.

**وعيب أمسكه الاختبار قبل التسليم — وهو أطرفها وأخطرها:** البحث عن idempotency
في الشيفرة طابق سطر تعليق في نموذج الاختبار نفسه يقول
`// a payment client with retries and no idempotency key` — أي أن **تعليقاً
يقرّر غياب الحماية قُرئ دليلاً على وجودها**. هذا شكل كل عيوب البحث النصّي التي
تستحقّ الوجود: الكلمات التي تصف الثغرة تشبه تماماً الكلمات التي تصف علاجها.
التعليقات تُجرَّد الآن قبل أي مطابقة.

---

### `risk-profile.mjs` — أين يكون الخطأ باهظاً، وهل تعرف الاختبارات ذلك

`ac-trace.mjs` يسأل كل معيار قبول سؤالاً واحداً: هل له اختبار قابل للفشل؟ **هذا
السؤال مسطّح.** يعطي الجواب نفسه لـ«العنوان يعرض اسم الوسيط» ولـ«التسوية تحوّل
١٥٠٠ ريال إلى حساب mada» — فتخرج تغطية ١٠٠٪ فوق مجموعة اختبارات كافية لأحدهما
وبعيدة كل البعد عن الكفاية للآخر.

والأسوأ من التضليل: **سوء التوزيع.** الجهد ينتشر بالتساوي، فتنال الأجزاء التي
يكلّف الخطأ فيها انتباه جهة تنظيمية نفسَ الاختبار السعيد الواحد الذي تناله تسمية
حقل.

```bash
node .cursor/tools/risk-profile.mjs profile          # كل معيار، مرتّباً، والقاعدة التي صنّفته
node .cursor/tools/risk-profile.mjs check            # بوابة ٥
node .cursor/tools/risk-profile.mjs explain AC-12
```

**ليست درجة.** الدرجات من نوع «الاحتمال ١-٥ × الأثر ١-٥» تبدو موضوعية وهي
مخترعة، وتتقادم في اليوم التالي لورشة العمل، ولا يستطيع أحد أن يجادل رقماً. هنا
**مستويات من قواعد صريحة**، وكل مستوى يطبع القاعدة التي أنتجته — **والكلمة التي
طابقت**:

```
money ("settlement") — moves or computes money
```

سطرٌ كهذا يمكن كسبه أو خسارته في ثانيتين: ترى فوراً أن الإشارة انطلقت من اسم
شاشة لا من فعل. هذا هو الفرق بين علامة غامضة وحجّة.

| المستوى | متى | ما تتطلّبه الأدلّة |
|---|---|---|
| T1 عادي | لا إشارة | اختبار واحد قابل للفشل — **القاعدة الحالية بلا تغيير** |
| T2 مرتفع | مال، هوية، صلاحية، أو فعل لا يُستردّ | + اختبار يؤكّد **مسار الفشل** |
| T3 حرج | التزام تنظيمي، أو (لا يُستردّ + مال/بيانات شخصية)، أو مال خلف قرار صلاحية | + تغطية على **طبقتين** لا طبقة واحدة |

**السُلَّم يرتفع فقط.** T1 هو القاعدة القائمة، فلا شيء تقوله هذه الأداة يمكن أن
يبرّر اختبار شيءٍ **أقلّ**. مستوى يبدو مرتفعاً أكثر من اللازم يكلّف بعض الاجتهاد
الزائد؛ مستوى يبدو منخفضاً هو الذي يستحقّ الجدال — وهذا هو الاتجاه الآمن للخطأ.

> **وما لا تراه:** معيارٌ باهظ لسببٍ لم يكتبه أحد يُقرأ T1. العلاج إعادة صياغة
> المعيار لا قائمة استثناءات — فمختبِرٌ بشري كان سيُضلَّل بالكلمات نفسها.
> منتَجها الحقيقي **ميزانية انتباه المراجِع**: قائمة مرتّبة بأين ينظر، لا حكم.

**عيبان اكتشفهما الاختبار قبل التسليم:** نافذة السياق كانت تتجاوز المعيار إلى
الذي يليه، فورث معيارُ «عنوان الشاشة» كلماتِ التسوية التنظيمية وصار حرجاً — أي
أن كل شيء صار حرجاً، وهو أسوأ ما يصيب ترتيباً. والثاني: اسم الاختبار كان يُقرأ
من الشريحة التالية، فبدا اختبارٌ سعيد وكأنه اختبار فشل لأن التعليق تحته كان
يقول «rejects». كلاهما في الاتجاه الخطر، وكلاهما مُصلَح.

---

### `release-evidence.mjs` — ماذا شُحن، وما الذي أثبته، ومن قال نعم

`lifecycle.mjs` يحكم **المنتج**: ست مراحل، ست بوابات، تُعتمَد مرّة. هذه الصورة
صحيحة لسؤال "هل قرّر أحدٌ ما هذا النظام"، وخاطئة تماماً لما يحدث بعدها: إصدار
كل أسبوعين، لأربع سنوات. اعتمِد البوابة السادسة في أوّل go-live، وينتهي تحكّم
المنصّة عندها — **الإصدار الثاني وكل ما بعده كان يمرّ عبر دورة حياة معتمَدة
سلفاً وإلى الأبد**.

الإصدار ليس مرحلة. هو حدث متكرّر له محتواه ودليله وتوقيعه.

```bash
node .cursor/tools/release-evidence.mjs cut --version v1.2.0
node .cursor/tools/release-evidence.mjs sign v1.2.0 --by "اسمك"
node .cursor/tools/release-evidence.mjs list
node .cursor/tools/release-evidence.mjs show v1.2.0
node .cursor/tools/release-evidence.mjs verify
```

**لا سطر واحد فيه مكتوب باليد.** المحتوى من git، حالات البوابات وأحكامها من
`state.json`، التجاوزات النشطة وطلبات التغيير المفتوحة من ملفّاتها، وملف
الحوكمة من مستندات المتطلبات. حقلٌ يستطيع أحدٌ كتابته هو حقلٌ سيصير خاطئاً.

**ما يرفضه `cut`:**

| الرفض | لماذا |
|---|---|
| شجرة عمل متّسخة | السجلّ سيسمّي كوميتاً ليس هو ما على القرص |
| `DEVELOPMENT` أو `TESTING` غير صافية | الإصدار هو حلقة ٤–٥ تُغلَق مرّة أخرى، لا خطوة بعدها |
| اعتماد `TESTING` **أقدم** من اعتماد `DEVELOPMENT` | بوّابتان خضراوان وترتيبهما خاطئ — الاختبارات وُقِّعت على كودٍ اعتُمِد بعدها |
| نسخة موجودة مسبقاً | السجلّ غير قابل للتعديل: إن تغيّر المحتوى فهذا إصدار آخر |

الرفض الثالث هو الأهم وأصعبها ملاحظة: دمج ميزة يحرّك `specs/features/`، فتصير
`DEVELOPMENT` بحالة `STALE` وحدها — لكن `tests/` لم يتغيّر اسم أيّ ملف فيه، فتبقى
`TESTING` معتمَدة. **فتشحن ميزةً بلا اختبار واحد، عبر بوابة اختبارٍ وُقِّعت قبل
أسابيع على كودٍ آخر.** بصمة المرحلة لا ترى هذا؛ تاريخان يريانه.

**وما يرفضه `sign`:** أي بوابة من الثلاث غير صافية، وأي **تجاوز نشط لم يسمِّه
المُوقِّع** بـ`--accept-override OV-XXXX`. توقيعٌ لم يُضطَرّ لذكر التجاوز هو
الطريق الذي يصير به تجاوزُ أسبوعين دائماً.

> **النزاهة، بصراحة:** كل سجلّ يحمل بصمة على محتواه، و`verify` يعيد حسابها. هذا
> يمسك تعديلاً يدوياً أو تلفاً — ولا يجعل السجلّ عصيّاً على العبث: من يستطيع
> تعديل الملف يستطيع إعادة حساب البصمة. **ما يجعله دليلاً هو الالتزام به في
> git.** البصمة سلكُ تنبيه، والتاريخ هو الدليل. التزِم `lifecycle/releases/`.

**وقاعدة واحدة تتغيّر:** عند بدء الإصدار التالي **لا تستعمل**
`rollback DEVELOPMENT` — فهو يصفّر `TESTING` و`PRODUCTION` معه ويرمي اعتمادين ما
زالا صحيحين. أعِد اعتماد المرحلة الجامدة **في مكانها**؛ `lifecycle.mjs` صار
يسمح بإعادة اعتماد مرحلة أسبق سبق اعتمادها، تحديداً كي لا تحتاج حلقة الإصدار
إلى مطرقة.

---

### `platform-metadata.mjs` — الأعداد محسوبة لا مكتوبة

```bash
node .cursor/tools/platform-metadata.mjs show           # الأعداد الحقيقية
node .cursor/tools/platform-metadata.mjs write          # -> platform-metadata.json
node .cursor/tools/platform-metadata.mjs check [--fix]  # أي وثيقة تدّعي رقماً لم يعد صحيحاً
node .cursor/tools/platform-metadata.mjs check --all    # مسح استشاري على كل الشجرة
```

`docs-lint` يقرأ Markdown فقط — ولهذا ظلّ `.claude-plugin/plugin.json` يعلن
«77 مهارة، 8 وكلاء، 5 خطّافات» طويلاً بعد أن توقّف كل ذلك عن الصحّة، لأنه JSON.
هذه الأداة تقرأ الاثنين.

`check` الافتراضي يفحص **ملفات البيان فقط** (README, AGENTS, CLAUDE, HANDBOOK,
البلجن…) حيث الرقم المجرّد لا يعني إلا الإجمالي، فالإصلاح التلقائي آمن.
أما `--all` فاستشاري ولا يعدّل شيئاً أبداً، لأن النثر يعدّ مجموعات فرعية بشكل
مشروع (عبارة «skills total» في `skill-graph.md` تَعُدّ المولّدات الموصولة بـ
`pattern-finder` وحدها، لا كل المكتبة) ويستشهد بأرقام القواعد («09 and 10 guard
rules» أرقامُ قاعدتين لا عدد قواعد) — وإعادة كتابتها إفساد لوثائق صحيحة
إرضاءً لمدقّق.

> ملاحظة: جدول الأسماء في الأداة إنجليزي، فالأعداد في هذا الملف العربي **ليست**
> مشمولة بالفحص وتُصحَّح يدوياً.

### `flag-debt.mjs` · `docs-lint.mjs` · `build-plugin.mjs`

```bash
node .cursor/tools/flag-debt.mjs scan          # أعلام ميزات منتهية الصلاحية -> exit 1
node .cursor/tools/docs-lint.mjs check         # روابط مكسورة، مهارات وهمية، أعداد قديمة
node .cursor/tools/docs-lint.mjs graph         # من يرتبط بمن
node .cursor/tools/build-plugin.mjs build      # يبني شجرة البلجن
node .cursor/tools/build-plugin.mjs check      # هل الشجرة متزامنة؟
```

`docs-lint` موجود لأن **إعدادات الوكيل هي الجزء الوحيد من المستودع بلا مترجم
ولا اختبارات**. مهارة أُعيدت تسميتها وما زالت مذكورة، أو عدد انحرف — لا شيء
منها يكسر شيئاً بصوت عالٍ؛ فقط تجعل المنصة خاطئة بهدوء، ونمط الفشل هو
"يبدو أن الوكيل صار أسوأ مؤخراً"، وهذا لا يُفتح له تذكرة.

---

## 11. الميموري بانك — 24 ملفاً على طبقتين

`memory-bank/` هو السياق الدائم للمشروع. مقسوم لطبقتين لأنهما تتغيّران
بمعدّلات مختلفة ويملكهما طرفان مختلفان.

### الطبقة الآلية (تحتهما)

| الملف | المالك | ملاحظة |
|---|---|---|
| `.cursor/cache/repo-map.json` | `/repo-discovery` | **بنيوي**. لا تعدّله يدوياً — يُعاد توليده |
| `.cursor/cache/feature-map.json` | `/feature-trace`, `/feature-inventory` | **سلوكي**. اكتب إليه عبر `feature-map.mjs` فقط |

### الطبقة الأولى — ديناميكية (تُولَّد من الكود الحيّ)

تصف **الحالة الفعلية لهذا المستودع الآن**. يكتبها `/context-sync` أول مرة،
وتبقيها القاعدة `00-memory-think` حديثة.

| الملف | المالك | يُحدَّث عند |
|---|---|---|
| `techContext.md` | `/context-sync` | حزمة جديدة، ترقية إطار |
| `systemPatterns.md` | `/context-sync` | تبنّي نمط معماري جديد |
| `projectbrief.md` | `/context-sync` | تغيّر نطاق المشروع |
| `productContext.md` | `/context-sync` | تغيّر غرض المنتج أو مستخدموه |
| `progress.md` | `/context-sync`, القاعدة `00` | بدء/إنهاء مهمة |
| `activeContext.md` | القاعدة `00` | كل جلسة |
| `techDebt.md` | `technical-debt-tracker` | اكتشاف/سداد دَين تقني |
| `WORKING_ON.md` | `speckit-git-feature` | إنشاء/دمج فرع ميزة |

### الطبقة الثانية — معايير ثابتة (يكتبها الفريق مرّة)

**معايير وأعراف**، لا لقطة من الكود. لا تُستخرَج من المستودع — إنسان يقرّر
ما تقوله، والمهارات والقواعد تقرؤها كحقيقة أساسية تُقاس عليها المخرجات.

`architecture.md` · `codingStandards.md` · `businessRules.md` ·
`technologyStack.md` · `databaseConventions.md` · `apiConventions.md` ·
`frontendConventions.md` · `backendConventions.md` · `securityStandards.md` ·
`performanceGuidelines.md` · `testingStandards.md` · `deploymentNotes.md` ·
`decisionLog.md` · `commonMistakes.md` · `glossary.md`

> `guard-write.mjs` **يمنع** الوكيل من الكتابة في ملفات الطبقة الثانية.
> إذا احتجت تغييراً فيها، اقترحه على المستخدم في المحادثة. وإذا طلبه المستخدم
> صراحةً، يُطبَّق بـ `CLAUDE_ALLOW_TIER2_EDIT=1`.
> هذا هو المكان الذي تهبط فيه ترقيات المراحل ١–٣.

---

## 12. القوالب — `templates/`

هذه ليست للمنصة، بل **لمستودع التطبيق الهدف**. هي الطبقة التي تحوّل قواعد
الحراسة إلى أخطاء ترجمة وإخفاقات CI، فتصمد سواء كتب الكود وكيل أو إنسان.

| المسار | يفرض |
|---|---|
| `templates/dotnet/Directory.Build.props` | تحذيرات كأخطاء، محلّلات، إعدادات موحّدة للحل |
| `templates/dotnet/Directory.Packages.props` | إدارة إصدارات الحزم مركزياً (CPM) |
| `templates/dotnet/BannedSymbols.txt` | رموز ممنوعة — وهنا تهبط مخرجات `/postmortem` |
| `templates/dotnet/.editorconfig` | قواعد التنسيق والتسمية |
| `templates/dotnet/ArchitectureTests/LayerBoundaryTests.cs` | حدود الطبقات كاختبار يُفشل البناء |
| `templates/dotnet/ArchitectureTests/ConventionTests.cs` | أعراف التسمية والبنية كاختبار |
| `templates/react/eslint.config.mjs` | حدود الاستيراد في React كخطأ lint |
| `templates/mutation/stryker-config.json` | اختبار الطفرات وعتبته |
| `templates/ci/quality-gates.yml` | "هل هذا الكود جيّد؟" — بناء، اختبار، فحص، تنسيق |
| `templates/ci/lifecycle-gates.yml` | "هل كان مسموحاً لهذا الكود أن يوجد بعد؟" — بوابات دورة الحياة في CI |

### لماذا `lifecycle-gates.yml` ضروري

`guard-phase.mjs` يمنع الكتابة قبل بوابة التصميم — لكن **داخل جلسة وكيل فقط**.
التزام (commit) من إنسان، أو تشغيل وكيل بـ `LIFECYCLE_OVERRIDE=1`، يتجاوزه
تماماً. قاعدة تُنفَّذ وقت التوليد فقط تحميك من الوكيل؛ قاعدة تُنفَّذ في CI
تحميك من الجميع.

الوظائف الأربع فيه: بوابة التصميم مقابل شجرة المصدر، مُخرَجات المرحلة الحالية،
سلامة الاعتمادات (مرحلة `APPROVED` لم تعد مُخرَجاتها موجودة)، وسلامة المنصة
(`docs-lint` + `flag-debt`).

---

## 13. الازدواجية بين `.cursor/` و`.claude/` — ليست مشكلة

قد تبدو `.claude/skills/` نسخة مكرّرة من `.cursor/skills/`. ليست كذلك.

| | Cursor | Claude Code |
|---|---|---|
| العقد | `AGENTS.md` | `CLAUDE.md` (يستورد `AGENTS.md`) |
| المهارات | `.cursor/skills/<n>/skill.md` — **المصدر** | `.claude/skills/<n>/SKILL.md` — **shim** |
| القواعد | `.mdc` تلقائية حسب النمط | مُعادة الصياغة في `CLAUDE.md` + حقن عند بدء الجلسة |
| الوكلاء | `.claude/agents/*.md` | **الملفات نفسها** |
| وصل الخطّافات | `.cursor/hooks.json` | `.claude/settings.json` |
| سكربتات الخطّافات | `.claude/hooks/*.mjs` | **الملفات نفسها** |
| MCP | `.cursor/settings.local.json` | `.mcp.json` |

**الإنفاذ يعمل على المضيفَين.** كان هذا القسم يقول إن الوكلاء والخطّافات
حكرٌ على Claude Code — ولم يعد ذلك صحيحاً: Cursor يدعم الوكلاء والمهارات
والقواعد والأوامر والخطّافات وMCP أصلياً. فالمنصة توصّل **نفس السكربتات**
للاثنين.

السكربتات محايدة تجاه المضيف لأن `_lib.mjs` يطبّع ما يختلفان عليه: أين مسار
الملف في الـ payload، وأين أمر الصدفة، وكيف يقول الخطّاف «امنع»، وكيف يحقن
سياقاً، وكيف يعرف جذر مساحة العمل. القاعدة تُعبَّر مرة واحدة ولا تتفرّع على
اسم المحرّر. نسختان من قاعدة، واحدة لكل مضيف، هي الطريق إلى توقّف إحداهما
عن العمل بصمت.

وربطٌ واحد أفضل فعلاً على Cursor: أوامر الصدفة تمرّ عبر `beforeShellExecution`،
وهو حدث مخصّص يعمل للأوامر فقط، ويحمل الأمر في الجذر، ويستطيع الرد بـ `ask`
لا `deny` فقط. أما Claude Code فيمرّر الحارس نفسه عبر مُطابِق `Bash` على
`PreToolUse`.

| السكربت | Claude Code | Cursor |
|---|---|---|
| `session-start.mjs` | `SessionStart` | `sessionStart` |
| `guard-write.mjs` | `PreToolUse` كتابة | `preToolUse` |
| `guard-phase.mjs` | `PreToolUse` كتابة | `preToolUse` |
| `guard-bash.mjs` | `PreToolUse` Bash | `beforeShellExecution` |
| `post-edit-verify.mjs` | `PostToolUse` كتابة | `afterFileEdit` |
| `stop-memory-check.mjs` | `Stop` | `stop` |

> `stop` يختلف بأكثر من الاسم: Claude Code يسمح بتنبيه استشاري عبر `exit 2`،
> بينما مخرج Cursor الوحيد هناك `followup_message` الذي **يرسل رسالة مستخدم
> جديدة تلقائياً**. اختطاف جلسة أحدهم لتوصيل تذكير أسوأ من ضياع التذكير،
> فيبقى الخطّاف على Cursor استشارياً على stderr.

الـ shim ملف من ~40 سطراً بلا أي محتوى، كل ما يقوله: "اقرأ
`.cursor/skills/<n>/skill.md` الآن واتبعه حرفياً". وجوده فقط ليكتشف Claude Code
المهارة ويوجّه إليها.

```bash
node .claude/hooks/sync-skills.mjs      # بعد أي إضافة/إعادة تسمية/حذف مهارة
```

الـ CI يُفشل الـ PR إذا انحرفت الـ shims. **لا تعدّل ملفات `.claude/skills/`
يدوياً أبداً.**

### `plugin/` — لماذا بناء لا رابط رمزي

الـ shim يعمل هنا لأن `.cursor/` موجود بجانبه. لكن البلجن يُركَّب داخل
مستودع شخص آخر حيث **لا وجود لـ `.cursor/`** — فيصير كل shim يشير إلى العدم،
وكل مهارة تفشل بصمت. وهذا أسوأ نمط فشل ممكن، لأن الوكيل ببساطة يتصرّف كأن
المنصة غير مُركَّبة.

لذلك شجرة البلجن **مكتفية بذاتها**: أجساد المهارات كاملة، والقواعد والأدوات
التي تشير إليها، وكل مسار مُعاد كتابته إلى `${CLAUDE_PLUGIN_ROOT}`.

```bash
node .cursor/tools/build-plugin.mjs build   # يولّد plugin/
node .cursor/tools/build-plugin.mjs check   # هل هي متزامنة؟ (يستخدمه CI)
```

**بيانان فوق شجرة واحدة.** التخطيط الذي يولّده البنّاء — `skills/<n>/SKILL.md`
و`rules/` و`agents/` و`hooks/` — هو بالضبط ما يتوقّعه المضيفان افتراضياً، فلا
يختلف إلا البيان:

| | البيان | وصل الخطّافات | MCP |
|---|---|---|---|
| Claude Code | `.claude-plugin/plugin.json` | `hooks/hooks.json` | `.mcp.json` |
| Cursor | `.cursor-plugin/plugin.json` | `hooks/cursor-hooks.json` | `mcp.json` |

وهو **Cursor Plugin** لا Agent Plugin: بحسب جدول مكوّنات Cursor نفسه، الـ Agent
Plugin يحمل المهارات وخوادم MCP **فقط** — فالقواعد والوكلاء والخطّافات، وهي
معظم هذه المنصة، لا تنتقل معه.

> قيد معروف: النثر داخل المهارات والقواعد يشير إلى `${CLAUDE_PLUGIN_ROOT}`،
> وCursor لا يعرّف هذا المتغيّر. القواعد والمهارات تُطبَّق هناك تلقائياً من
> البيان، فالأثر محصور في أسطر أوامر الأدوات التي قد تطبعها مهارة.

> `plugin/` **مولّد ومُلتزَم به** في نفس الوقت — لأن marketplace بمصدر محلي
> `./plugin` يجب أن يستطيع جلبه. لا تعدّله يدوياً؛ عدّل المصدر وأعد البناء.

---

## 14. خوادم MCP

`.mcp.json` (مُلتزَم به، لـ Claude Code) و`.cursor/settings.local.json`
(غير مُلتزَم به، لـ Cursor) يعرّفان نفس الخوادم الأربعة:

| الخادم | الاستخدام | متغيّر البيئة |
|---|---|---|
| `github` | الخادم الرسمي البعيد لـ GitHub | `GITHUB_PAT` — صلاحيات دقيقة، للقراءة افتراضياً |
| `postgres` | Postgres MCP Pro | `POSTGRES_READONLY_URL` — **يجب أن يكون دور SELECT فقط** |
| `context7` | توثيق المكتبات | `CONTEXT7_API_KEY` — اختياري، يرفع حدود المعدّل |
| `playwright` | أتمتة المتصفح | — |

> **تحذير لمن يشغّل نسخة قديمة من المنصة:** الخادمان
> `@modelcontextprotocol/server-github` و`@modelcontextprotocol/server-postgres`
> اللذان كانا مُوصى بهما سابقاً **مهجوران وغير مُصانَين**، وخادم Postgres شحن
> ثغرة حقن SQL تجاوزت وضع القراءة فقط الخاص به. حدّث إعدادك المحلي **ودوّر
> ذلك الاعتماد**.

التفاصيل والقائمة المستبعدة عمداً:
[`.cursor/docs/mcp-ecosystem.md`](.cursor/docs/mcp-ecosystem.md)

في المرحلة ٣، إذا كان خادم Figma MCP موصولاً، تقرأ منه مهارة
`/ux-design-bridge` **بنية** التصميم (أسماء الرموز، المكوّنات، قيم المسافات)
بدل وصف لقطة شاشة — وهذا ما تستطيع المرحلة ٤ التوليد منه فعلاً.

---

## 15. سير عمل كامل: من فكرة إلى إنتاج

المثال: *"منصّة يشتري عبرها العملاء وثائق تأمين المركبات ويديرونها إلكترونياً"*.

```bash
node .cursor/tools/lifecycle.mjs init --name "motors-online"
```

### المرحلة ١ — المتطلبات

```
/product-brief   منصّة يشتري عبرها العملاء وثائق تأمين المركبات ويديرونها إلكترونياً
/persona-gen
/product-requirements
/user-story-map
```

توقّع 10 أسئلة على الأقل، كل واحد بخيارات معنونة. هذه القاعدة `05` — وهي
أرخص ساعة في المشروع.

```bash
node .cursor/tools/lifecycle.mjs check REQUIREMENTS
```
```
/lifecycle-gate REQUIREMENTS
```
```bash
node .cursor/tools/lifecycle.mjs approve REQUIREMENTS --by "اسمك"
node .cursor/tools/lifecycle.mjs advance
```

### المرحلة ٢ — التحليل

```
/domain-model-gen
/use-case-gen
/business-rules-gen
/risk-register
```

ثم راجع الـ diff المقترح لـ `memory-bank/businessRules.md` و`glossary.md`
ووافق عليه. هذا ما يجعل القاعدة `07` تفرض متطلبات التدقيق في المرحلة ٤.

### المرحلة ٣ — التصميم

```
/solution-architecture
/threat-model     "التصميم"
/api-contract-design
/data-model-design
/ux-design-bridge
```

`/solution-architecture` يعرض بدائل معمارية **وينتظر**. لن يختار عنك.
`/threat-model` **ليس اختيارياً** — المنتج يمسّ المال وبيانات شخصية ومصادقة،
وبوابة تصميم بلا مخرجات STRIDE هي NO-GO تلقائي.

```bash
node .cursor/tools/lifecycle.mjs approve DESIGN --by "اسمك"
```
**هذا الاعتماد يفتح `guard-phase.mjs`.** قبله، كل كتابة تحت `src/` تفشل بـ
`exit 2` — وهذا التصميم يعمل، لا خلل.

### المرحلة ٤ — التطوير

```
/feature-pipeline status
/feature-pipeline next
```

لكل ميزة يشغّل السلسلة ويتوقّف عند كل قرار يخصّك:

```
/speckit-analyze → /speckit-clarify → /speckit-constitution → /speckit-plan
  → /speckit-specify → /speckit-implement <TASK> → /task-verify
  → /speckit-checklist → /speckit-git-commit
```

> مهمة تنتقل إلى Done **فقط** بحكم DONE من `/task-verify`، لا من مجموعة
> اختبارات خضراء. حين يكتب وكيل واحد الكود واختباراته، الأخضر يثبت أنهما
> متّفقان — لا أن أياً منهما صحيح. هذا هو سبب وجود تلك البوابة.

### المرحلة ٥ — الاختبار

```
/test-strategy
/e2e-test-gen    <الرحلة>
/load-test-gen   <نقطة النهاية>
/compliance-audit
```

```bash
node .cursor/tools/ac-trace.mjs check specs/features/<slug>.md
node .cursor/tools/ac-trace.mjs lint
```

تغطية معايير القبول 100% غير قابلة للتفاوض. وفوّض المسح الأمني إلى الوكيل
`security-auditor` — يقرأ ملفات أكثر بكثير مما يجب أن يدخل السياق الرئيسي.

راجع الـ PR بمراجع مستقل. مراجعة كود GitHub Copilot تقرأ `AGENTS.md`، فتقيّدها
قواعدك أيضاً — لكن يوقّع إنسان على أي شيء يمسّ المال أو المصادقة.

### المرحلة ٦ — الإنتاج

```
/deployment-pipeline-gen both
/operability-gen             <الميزة>
/release-safety              <التغيير>
/production-readiness-review <النطاق>
/go-live plan
```

ثم في النافذة الزمنية: `/go-live execute` ثم `/go-live verify`.

> `/production-readiness-review` يعيد Go/No-Go مانعاً. **لا تليّن NO-GO أبداً**
> — بوابة لم تمنع شيئاً قط ليست ضابطاً، بل استمارة.

كل ما يمسّ الإنتاج **يشغّله المستخدم**. الترحيلات على اتصال غير محلي يمنعها
خطّاف Bash، وهذا المنع صحيح.

### بعد الإطلاق

| الحدث | الأمر |
|---|---|
| حدث عطل | `/postmortem` — المخرج حارس وقت ترجمة، لا وثيقة |
| شُحن إصدار | `/changelog-gen`, `/release-notes-gen` |
| هل ينفع هذا كلّه؟ | `/delivery-metrics` |
| يبدأ الإصدار التالي | `lifecycle.mjs rollback DEVELOPMENT --reason "الإصدار 2"` |

---

## 16. العمل على مستودع قائم (Brownfield)

الحالة الأشيع فعلياً. الترتيب يهمّ:

```bash
node .cursor/tools/lifecycle.mjs init --name "المنتج" --existing
```

```
/repo-discovery full        # ابنِ الخريطة البنيوية
/context-sync               # املأ الميموري بانك من الكود الفعلي
/feature-inventory full     # ما الذي يفعله هذا النظام أصلاً؟
/feature-trace "<الميزة الأخطر>"      # كيف تعمل واحدة منها
/impact-analysis "<التغيير المقترح>"   # ما الذي سينكسر
/work-breakdown "<التغيير المقترح>"    # قسّمه لمهام قابلة للإنهاء
```

ثم قبل الوصول للإنتاج:

```
/threat-model "<التصميم>"
/operability-gen "<الميزة>"
/release-safety "<التغيير>"
/production-readiness-review "<النطاق>"
```

وأخيراً ركّب بوابات البناء من `templates/` حتى تصمد القواعد في CI لا وقت
التوليد فقط.

الدليل التفصيلي للنموذج:
[`.cursor/docs/APPLY-TO-PROJECT.md`](.cursor/docs/APPLY-TO-PROJECT.md)

---

## 17. الصيانة — ماذا تشغّل ومتى

| متى | الأمر |
|---|---|
| بداية كل جلسة | `/repo-discovery quick` |
| تغيير كبير عابر للملفات | `/context-builder [الوصف]` |
| بعد إضافة/إعادة تسمية مهارة | `node .claude/hooks/sync-skills.mjs` |
| بعد أي تعديل على مهارة أو قاعدة أو وثيقة | `node .cursor/tools/build-plugin.mjs build` |
| قبل أي PR على المنصة | `node .cursor/tools/docs-lint.mjs check` |
| دورياً | `node .cursor/tools/flag-debt.mjs scan` |
| كل ربع | `/skill-maturity-audit all` · `/platform-health-validator` |
| قبل الإصدار | `/enterprise-report-gen production-readiness [النطاق]` |
| بعد الشحن | حدّث `progress.md`؛ `/changelog-gen` عند الحاجة |

### قبل إضافة مهارة جديدة — اقرأ هذا

**96 مهارة تكلّف ~9 آلاف رمز من السياق دائم التفعيل** في Claude Code، ودقّة
التوجيه **تتدهور** مع الأوصاف شبه المتطابقة. المهارة الجديدة ليست مجانية.

قبل الإضافة:

1. شغّل `/skill-maturity-audit all` — ربما توجد مهارة تغطّي الحاجة.
2. اسأل: هل هذا **سؤال مختلف**، أم صياغة أخرى لسؤال موجود؟
3. اكتب الوصف أولاً. إن لم تستطع تمييزه في جملة عن أقرب مهارة قائمة، فهو
   ليس مهارة جديدة.
4. أضِف في `.cursor/skills/` فقط، ثم `sync-skills.mjs`، ثم `build-plugin.mjs`.

سجلّ ما قُبل وما رُفض من ~150 إضافة مقترحة ولماذا:
[`.cursor/docs/PROPOSAL-REVIEW.md`](.cursor/docs/PROPOSAL-REVIEW.md)

---

## 18. الأخطاء الشائعة وحلولها

| العَرَض | السبب | الحل |
|---|---|---|
| `BLOCKED: ... DESIGN gate has not passed` | العمل سابق لمرحلته | `lifecycle.mjs check DESIGN` ثم `/lifecycle-gate`. **ليس** `LIFECYCLE_OVERRIDE` |
| `approve` يرفض | مُخرَج ناقص أو ما زال قالباً | مخرجات `check` تسمّي أيّها |
| `BLOCKED: lifecycle/state.json ... owned by lifecycle.mjs` | تعديل الحالة يدوياً | استخدم الأداة |
| `BLOCKED: memory-bank/architecture.md is Tier 2` | ترقية صامتة | اعرض الـ diff، اسأل، ثم `CLAUDE_ALLOW_TIER2_EDIT=1` |
| الوكيل يبدأ بالبرمجة في المرحلة ٢ | دورة الحياة لم تُهيّأ | `lifecycle.mjs init` |
| الوكيل يخترع أنواعاً أو حزماً | خريطة قديمة | `/repo-discovery full` + القاعدة `10` |
| الوكيل يتخطّى المهارات | لم يقرأ العقد | أشِر إلى `@AGENTS.md` و`@skill-catalog.md` في أول رسالة |
| الميموري بانك قديم | لم يُزامَن | `/context-sync` |
| حرّاس المعمارية لا تعمل | القواعد `02`–`08` مرتبطة بأنماط ملفات | افتح ملف `.cs` / `.tsx` فعلياً |
| `dotnet add package` ممنوع | القاعدة `10` + خطّاف Bash | **توقّف واسأل** — لا تلتفّ حوله |
| `build-plugin` يفشل بـ EPERM | صلاحيات حذف على المجلد | امنح صلاحية الحذف، أو شغّله محلياً |

---

## 19. المزيد

| الوثيقة | الغرض |
|---|---|
| [HANDBOOK.md](HANDBOOK.md) | النسخة الإنجليزية الكاملة |
| [AGENTS.md](AGENTS.md) | عقد الوكيل — انسخه إلى جذر كل مستودع تطبيق |
| [CLAUDE.md](CLAUDE.md) | نقطة دخول Claude Code |
| [.cursor/docs/LIFECYCLE.md](.cursor/docs/LIFECYCLE.md) | المراحل الست وبواباتها ومُلّاكها ومُخرَجاتها |
| [.cursor/docs/IDEA-TO-PRODUCTION.md](.cursor/docs/IDEA-TO-PRODUCTION.md) | الدليل التشغيلي: فكرة واحدة، كل الأوامر بالترتيب |
| [.cursor/docs/START-HERE.md](.cursor/docs/START-HERE.md) | "أريد أن أفعل X" ← أي مهارة |
| [.cursor/docs/skill-catalog.md](.cursor/docs/skill-catalog.md) | فهرس المهارات الـ96 والتوجيه التلقائي |
| [.cursor/docs/skill-graph.md](.cursor/docs/skill-graph.md) | تبعيات المهارات وتوصيلاتها |
| [.cursor/docs/shared-execution-pipeline.md](.cursor/docs/shared-execution-pipeline.md) | الترتيب القانوني: اكتشاف ← نمط ← توليد ← تحقّق |
| [.cursor/docs/NEW-PROJECT.md](.cursor/docs/NEW-PROJECT.md) | تهيئة مستودع تطبيق جديد |
| [.cursor/docs/APPLY-TO-PROJECT.md](.cursor/docs/APPLY-TO-PROJECT.md) | تهيئة مستودع قائم — يُعطى للنموذج |
| [.cursor/docs/DUAL-AGENT-SETUP.md](.cursor/docs/DUAL-AGENT-SETUP.md) | كيف يتشارك Cursor وClaude Code مصدراً واحداً |
| [.cursor/docs/mcp-ecosystem.md](.cursor/docs/mcp-ecosystem.md) | خوادم MCP وتصحيحات 2026 |
| [templates/README.md](templates/README.md) | بوابات البناء — القواعد كأخطاء ترجمة |

---

*كل الأرقام في هذا الملف مأخوذة من المستودع مباشرة بتاريخ 6 سبتمبر 2026.
عند إضافة مهارة أو قاعدة، شغّل `node .cursor/tools/docs-lint.mjs check` —
سيمسك أي عدد انحرف هنا.*
