{
  "version": 1,

  "//": "Which files may be written in which phase. Read by .claude/hooks/guard-phase.mjs on Cursor's preToolUse and Claude Code's PreToolUse. Hand-edited on purpose - this is a policy, not a generated artifact.",

  "//why": "The design gate answered one question: may source be written yet? That left everything else ungoverned. Nothing stopped an agent writing production Terraform during REQUIREMENTS, or a deployment manifest before a single test existed. Both are code that outruns the decisions it depends on, which is the whole thing this layer exists to prevent.",

  "//earliest": "`earliest: X` means the phase BEFORE X must be cleared (APPROVED or INHERITED). So source needs DESIGN cleared - exactly the rule that already existed, generalised rather than replaced. A rule whose earliest is REQUIREMENTS has no prerequisite and never blocks.",

  "//not-blind": "This is deliberately narrow. Writing a test, a spec, a document or a memory-bank entry early is good practice, not a violation, so those are never blocked. Only artifacts that encode a decision from a later phase are gated.",

  "alwaysAllow": [
    "docs/**",
    "specs/**",
    "memory-bank/**",
    "lifecycle/**",
    "schemas/**",
    "templates/**",
    "scripts/**",
    "tests/**",
    "test/**",
    "e2e/**",
    ".cursor/**",
    ".claude/**",
    "*.md",
    "*.txt",
    ".gitignore",
    ".editorconfig"
  ],

  "rules": [
    {
      "id": "application-source",
      "match": ["src/**", "backend/**", "frontend/**", "client/**", "server/**", "app/**", "api/**", "web/**"],
      "extensions": [".cs", ".csproj", ".sln", ".fs", ".vb", ".ts", ".tsx", ".js", ".jsx", ".vue", ".svelte", ".razor", ".cshtml", ".sql"],
      "earliest": "DEVELOPMENT",
      "why": "Code written before the design gate implements a design nobody approved. This is the original rule and its behaviour is unchanged."
    },
    {
      "id": "database-migrations",
      "match": ["**/Migrations/**", "**/migrations/**", "**/*.migration.sql", "db/migrate/**"],
      "earliest": "DEVELOPMENT",
      "why": "A migration is the physical schema made real. It follows docs/design/database-design.md; written before it, it becomes the design by default."
    },
    {
      "id": "infrastructure-as-code",
      "match": ["**/*.tf", "**/*.tfvars", "**/*.bicep", "infra/**", "infrastructure/**", "**/*.cloudformation.json"],
      "earliest": "DESIGN",
      "why": "IaC encodes the deployment architecture, which is a phase 3 decision (/dotnet-iac-gen). Written during analysis it silently settles what the architecture review was supposed to decide."
    },
    {
      "id": "ci-pipelines",
      "match": [".github/workflows/**", "azure-pipelines.yml", "azure-pipelines/**", ".gitlab-ci.yml", "Jenkinsfile", ".circleci/**"],
      "earliest": "TESTING",
      "why": "A pipeline that builds and tests needs something to build and test. Generated by /deployment-pipeline-gen once the code exists."
    },
    {
      "id": "deployment-manifests",
      "match": ["k8s/**", "kubernetes/**", "helm/**", "charts/**", "deploy/**", "docker-compose.prod*", "**/*.deployment.yaml"],
      "earliest": "PRODUCTION",
      "why": "Deploying is the last gate for a reason. A manifest written before the testing gate is a plan to ship something nobody has proven works."
    }
  ]
}
